# Phase 40: Unify Stability and Group Velocity in the Sweep Optimization Pipeline

**Goal:** Bring group velocity (GV) analysis into the sweep optimization pipeline as a secondary objective alongside the existing eigenvalue stability check, so that optimal stencil parameters minimize GV error among the stable feasible set. Also expose GKS-style group-velocity diagnostics as advisory output.

**Depends on:** Phase 39 (complete — sweep extraction and code simplification done)

**Background — current state of the two pipelines:**

| Aspect | Eigenvalue stability | Group velocity |
|---|---|---|
| Canonical API | `phs.stability_eigenvalue(n,p,q,eps,kernel,nu,nextra)` → `max Re(λ(-D_bc))` (`phs.py:807`) | `interior_group_velocity(p,nu,xi)`, `boundary_group_velocity(...)`, `psi_sweep_group_velocity(...)`, `gks_group_velocity_check(D,xi)` (`group_velocity.py:504,533,718,964`) |
| Threshold / scalar | `< STABILITY_TOL = 1e-10` (binary feasible/infeasible) | `profile.gv_error` array, `profile.cutoff_xi`, `PsiSweepResult.min_C`, `has_sign_reversal` (no canonical scalar yet) |
| Cost per call | `np.linalg.eigvals` on dense `n×n` (O(n³), n≤160 → sub-ms) | O(N_xi × stencil_width) numpy ops (microseconds) |
| Sweep integration | All 6 sweeps consume it via `sweeps/_common.py` constants | **Zero** — no sweep imports `group_velocity` |

**Multi-objective prior art:** `tension_penalty_sweep.py` (`eval_point` at lines 36–58) already handles two objectives via the *feasible-then-minimize* pattern: stability is a hard constraint (`se < STABILITY_TOL`); among feasible points, the conservation deficit is minimized. **This is the exact pattern we will reuse for GV.** GV becomes the secondary objective (or third, in penalty sweeps), never overriding stability.

**Why not GV-as-stability-replacement:** `gks_group_velocity_check` is *necessary not sufficient* for instability (test docstring at `test_group_velocity.py:1015–1017`). It will be exposed as an advisory diagnostic, not a feasibility gate.

**`known_values.json` compatibility:** The regression tests in `test_phs.py` (lines ~1358–1630) only access named keys (`{kernel}.epsilon`, `tension.sigma`, `params`, `stable_at`). Adding new keys (e.g. `tension.gv_error`, `tension_gv`, etc.) is non-breaking; existing tests will ignore them.

**Read first:**
- `scripts/stencil_gen/sweeps/_common.py` (`STABILITY_TOL`, `SCHEME_PARAMS`, `SweepResult`, helpers)
- `scripts/stencil_gen/sweeps/tension_penalty_sweep.py` (the multi-objective prior art — `eval_point`, `run_joint_sweep_coarse`)
- `scripts/stencil_gen/sweeps/tension_sweep.py` (simplest 1D sweep — testbed for new GV objective)
- `scripts/stencil_gen/sweeps/epsilon_sweep.py` (parallel structure)
- `scripts/stencil_gen/sweeps/__main__.py` (CLI dispatch — where new subcommands go)
- `scripts/stencil_gen/sweeps/known_values.json` (schema, current keys)
- `scripts/stencil_gen/stencil_gen/group_velocity.py` (lines 504–730 — interior/boundary/cut-cell APIs)
- `scripts/stencil_gen/stencil_gen/phs.py` (lines 807–840 — `stability_eigenvalue` and `_from_matrix`)
- `scripts/stencil_gen/tests/test_phs.py` (lines 1358–1630 — `TestRegression*` JSON loaders)

**Test commands:**
```bash
# Targeted: just the new objectives module
cd scripts/stencil_gen && uv run pytest tests/test_sweep_gv_objectives.py -x -q

# Smoke: each sweep with --include-gv at minimal resolution (each <30s)
cd scripts/stencil_gen && uv run python -m sweeps tension --scheme E2 --n-sigma 5 --include-gv
cd scripts/stencil_gen && uv run python -m sweeps epsilon --scheme E2 --kernel gaussian --n-eps 5 --include-gv

# Regression tests (must still pass)
cd scripts/stencil_gen && uv run pytest tests/test_phs.py -x -q -k "TestRegression"
```

---

## Items

### 40.1 — Shared GV objective helpers in `sweeps/`

- [x] **40.1a** Create `sweeps/gv_objectives.py` with five thin scalar wrappers around the existing `group_velocity` API:
  - `interior_gv_error_max(p: int, nu: int = 1, n_xi: int = 200) -> float` — calls `interior_group_velocity`, returns `float(np.max(np.abs(profile.gv_error)))`.
  - `interior_cutoff_fraction(p: int, nu: int = 1, n_xi: int = 200) -> float` — returns `profile.cutoff_xi / np.pi` (1.0 = ideal, lower = earlier parasitic onset).
  - `boundary_gv_error_max(p, q, nextra, nu, sigma, kernel, n_xi=200) -> float` — calls `boundary_group_velocity`, returns the max over rows of `np.max(np.abs(prof.gv_error))`.
  - `cutcell_gv_min_C(scheme_params, psi_values, alpha_values, n_xi=200) -> tuple[float, bool]` — calls `psi_sweep_group_velocity`, returns `(result.min_C, result.has_sign_reversal)`.
  - `gv_score_from_matrix(D: np.ndarray, n_xi: int = 200) -> dict` — extracts boundary rows from a constructed `D` matrix and returns `{"max_gv_error": ..., "min_cutoff_xi": ...}` (this is the entry point sweeps that already build `D` will use to avoid rebuilding). Uses a leading-rows scanner (row's leftmost nonzero column == 0) to identify the left-boundary block from D alone.
  - Each function has a one-line docstring describing its semantics. Module docstring explains the "feasible-then-minimize" contract.
  - File: `scripts/stencil_gen/sweeps/gv_objectives.py` (new) — **done**
  - Verified: `uv run python -c "from sweeps.gv_objectives import ...; ..."` returns finite positive values; `gv_score_from_matrix(D)["max_gv_error"]` matches `boundary_gv_error_max` to machine precision for the E2 tension case at sigma=6.0.

- [x] **40.1b** Add unit tests for the five helpers in `tests/test_sweep_gv_objectives.py`:
  - For each helper, one assertion that it returns a finite positive float (or correct tuple) at known inputs (E2 interior, E4 boundary at the stored optimum sigma from `known_values.json`).
  - Note: `@pytest.mark.fast` is **not** applied because the project's `pyproject.toml` only registers a `slow` marker; unmarked tests already run in the default suite, and adding an unregistered marker would emit pytest warnings. Tests are simply not marked `slow`.
  - File: `scripts/stencil_gen/tests/test_sweep_gv_objectives.py` (new) — **done**
  - Verified: `uv run pytest tests/test_sweep_gv_objectives.py -x -q` → 7 passed in 0.92s. `tests/test_phs.py -k TestRegression` → 15 passed (no regressions).
  - Follow-up for 40.7b / 40.8b: this file already exists; those items just append tests.

- [x] **40.1c** Add the missing `cutcell_gv_min_C` unit test (gap from 40.1b — only four of the five helpers were actually tested):
  - Review pass on commit `8fb9f68` confirmed `tests/test_sweep_gv_objectives.py` has zero references to `cutcell_gv_min_C`. Plan item 40.1b explicitly required "For each helper, one assertion that it returns a finite positive float (or correct tuple) at known inputs"; this helper's tuple contract is currently uncovered, and downstream item 40.5a depends on it.
  - Use a small fixture: `from stencil_gen.temo import E2_1` for `scheme_params`, an empty `alpha_values={}` (the helper's `psi_sweep_group_velocity` defaults missing alpha symbols to 0 — see `group_velocity.py:763`), and `psi_values=np.linspace(0.05, 0.95, 5)` to keep runtime under one second.
  - Assertions: returned object is a 2-tuple; `np.isfinite(min_C)`; `isinstance(has_sign_reversal, bool)` (not `np.bool_`, since the helper wraps with `bool(...)`).
  - File: `scripts/stencil_gen/tests/test_sweep_gv_objectives.py` — **done**
  - Verified: `uv run pytest tests/test_sweep_gv_objectives.py -x -q` → 8 passed in 0.88s. The new test asserts only `np.isfinite(min_C)` (not strictly positive) since `psi_sweep_group_velocity` returns the most-negative C across the psi sweep — at E2_1 with empty alpha it's −32.3.

### 40.2 — Integrate GV into `tension_sweep` (testbed sweep)

- [x] **40.2a** Add `--include-gv` CLI flag to `tension_sweep.py`:
  - Default `False` so existing invocations behave identically.
  - When set, after the existing stability scan, evaluate `boundary_gv_error_max(p, q, nextra, nu, sigma, "tension")` for every coarse-grid sigma and store alongside the `(sigma, stab_eig)` tuples.
  - Argument plumbing only — no behavior change yet beyond a new column. Keep the change <60 lines.
  - File: `scripts/stencil_gen/sweeps/tension_sweep.py` — **done**
  - Implementation: `sweep_stability` now returns `(stab_results, gv_by_sigma)` where `gv_by_sigma: dict[float, float] | None` is populated once per sigma (grid-size-independent) when `include_gv=True`, else `None`. `run_tension_sweep` accepts `include_gv=` kwarg and threads it through; the returned summary dict gains a `gv_by_sigma` key. `main()` adds `--include-gv`, and `sweeps/__main__.py` adds the same flag on the tension subparser with passthrough. No existing output format changed — 40.2b owns the printing work.
  - Verified: `uv run python -m sweeps tension --scheme E2 --n-sigma 5 --include-gv` runs end-to-end without error; `uv run python -m sweeps tension --scheme E2 --n-sigma 5` (no flag) behaves identically to before; `uv run pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -k "TestRegression or gv_objectives"` → 23 passed (no regressions).

- [x] **40.2b** Print GV error column in `tension_sweep` table when `--include-gv` is set:
  - Extend `print_sweep_table` call (or wrap output) so the per-(n, sigma) table gains a `gv_err` column.
  - Among feasible (`stab_eig < STABILITY_TOL`) sigmas, log a "Best by GV error: sigma=…, gv_err=…" line in addition to the existing "widest stable range" output.
  - File: `scripts/stencil_gen/sweeps/tension_sweep.py` — **done**
  - Implementation: `print_sweep_table` in `_common.py` gained an optional `gv_by_param` kwarg (additive, default `None`); when present, the per-n table renders a fourth `gv_err` column. `tension_sweep.py` passes `gv_by_sigma` through and, after `report_stable_ranges`, computes the intersection of stable sigmas across all grid sizes (the feasible set) and prints `Best feasible by GV error: sigma=…, gv_err=…`. The feasible set being empty is reported, not crashed.
  - Verified: `uv run python -m sweeps tension --scheme E2 --n-sigma 5 --include-gv` shows the new column and `Best feasible by GV error: sigma=20.000000, gv_err=2.074641e+00`. The same command without `--include-gv` is byte-identical to the prior format (no column added). `uv run pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -k "TestRegression or gv_objectives"` → 23 passed.

- [x] **40.2c** Persist GV-optimal sigma to `known_values.json` under a new sub-key:
  - When `--update-known-values` AND `--include-gv` are both set, write `kv[scheme_key]["tension"]["gv_error"] = best_gv_err` and `kv[scheme_key]["tension_gv"] = {"sigma": ..., "gv_error": ..., "stable_at": [...]}`.
  - This is additive: the existing `tension.sigma` (widest-stability optimum) is unchanged. The new `tension_gv.sigma` is the GV-optimal feasible sigma.
  - File: `scripts/stencil_gen/sweeps/tension_sweep.py` — **done**
  - Implementation: `run_tension_sweep` now cross-checks the GV-optimal feasible sigma at grid sizes `{20,40,80,160}` (mirroring the existing stability-optimum cross-check) and returns `gv_sigma`, `gv_error`, `gv_stable_at` in the summary. `main()` writes `tension.gv_error` (additive on the existing entry) and a new top-level `tension_gv` dict only when both `--update-known-values` and `--include-gv` are set; without `--include-gv`, the existing `tension` entry is written without the `gv_error` key (unchanged behavior). Without `--update-known-values`, nothing is written.
  - Verified: `uv run python -m sweeps tension --scheme E2 --n-sigma 5 --include-gv --update-known-values` wrote both `E2_1.tension.gv_error = 2.074641…` and `E2_1.tension_gv = {sigma: 20.0, gv_error: 2.074641…, stable_at: [20,40,80,160]}`; `known_values.json` restored from pre-test snapshot after verification (the low-resolution `--n-sigma 5` run would have overwritten the stability optimum at 6.0 with 3.483637, so this smoke run was not committed to the JSON). `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -k "gv_objectives or TestRegression"` → 23 passed. The no-flag invocation (`--n-sigma 5` without `--include-gv`) still produces the same output as before.

- [x] **40.2d** Fix non-additive overwrite of `tension.gv_error` in `tension_sweep.main()`:
  - Review pass on commit `74df8c7` confirmed: when `--update-known-values` is invoked **without** `--include-gv`, `main()` rebuilds `tension_entry = {"sigma": ..., "stable_at": ...}` from scratch and assigns it to `kv[scheme_key]["tension"]`, silently dropping any `gv_error` key that a prior `--include-gv` run had populated. The 40.2c verification never exercised this round-trip — it only tested `--include-gv --update-known-values` once and then restored the JSON.
  - Fix: `main()` now reads the existing `kv[scheme_key].get("tension", {})` and `kv[scheme_key].get("tension_gv", {})` and merges only the keys this invocation owns (`sigma`, `stable_at`, and conditionally `gv_error`). Earlier-run keys survive a subsequent non-GV `--update-known-values` invocation.
  - File: `scripts/stencil_gen/sweeps/tension_sweep.py` — **done**
  - Verified: snapshot `known_values.json`; run `uv run python -m sweeps tension --scheme E2 --n-sigma 5 --include-gv --update-known-values` (writes `E2_1.tension.gv_error = 2.074641…` and `E2_1.tension_gv`), then `uv run python -m sweeps tension --scheme E2 --n-sigma 5 --update-known-values` (no `--include-gv`). After step 2, `E2_1.tension.gv_error` is still `2.074641143264805` and `E2_1.tension_gv` is unchanged. Snapshot restored. `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -k "TestRegression or gv_objectives"` → 23 passed.
  - **Carry-over note:** the same merge pattern must be used when implementing 40.3c (epsilon sweep), 40.4c (tension-penalty), and 40.5c (footprint) — they all face the identical "additive on existing entry" contract.

- [x] **40.2e** Add an automated regression test for the `tension_sweep.main()` merge pattern:
  - Review pass on commit `f9ce69b` confirmed the 40.2d fix is only exercised by manual "snapshot → run twice → inspect JSON → restore" verification. There is no test that will fail if a future refactor (or the carry-over implementations in 40.3c/40.4c/40.5c) re-introduces a non-merging assignment to `kv[scheme_key]["tension"]`. The 40.2c bug was exactly this kind of silent regression; without a test, 40.2d's guarantee is review-dependent.
  - Implementation: `test_tension_sweep_main_merges_known_values` in `tests/test_sweep_gv_objectives.py` monkeypatches `sweeps._common.KNOWN_VALUES_PATH` to a `tmp_path / "known_values.json"`, seeds it via `_seed_kv()` with `{"E2_1": {"tension": {"sigma": 6.0, "stable_at": [20,40,80], "gv_error": 1.234, "preexisting_extra_key": "survive"}, "tension_gv": {"sigma": 5.5, "gv_error": 1.234, "stable_at": [20,40]}}}`, then runs `tension_sweep.main` twice: first without `--include-gv` (asserts `tension.gv_error == 1.234`, `tension.preexisting_extra_key == "survive"`, and `tension_gv` unchanged byte-for-byte), then with `--include-gv` (asserts `preexisting_extra_key` still present, `tension.gv_error` refreshed to a finite float, and `tension_gv` refreshed). Note: tension_sweep does not re-export `KNOWN_VALUES_PATH`, so monkeypatching only `_common` is sufficient — the plan's "and the re-export in tension_sweep" was incorrect. The seed helper is a module-local `_seed_kv(path)`; the carry-over items 40.3c/40.4c/40.5c can extract it into a parameterized helper if/when their sweeps land.
  - File: `scripts/stencil_gen/tests/test_sweep_gv_objectives.py` — **done**
  - Verified: `uv run pytest tests/test_sweep_gv_objectives.py -x -q` → 9 passed in 1.09s. `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q -k "TestRegression or gv_objectives or sweep_gv"` → 24 passed in 1.33s. The two `tension_sweep.main` invocations together add ~1s to the test runtime, well under the 2s budget.

- [x] **40.2f** Extend the 40.2e regression test to also pin the `tension_gv` merge:
  - Review pass on commit `7071482` confirmed: `_seed_kv` places the `preexisting_extra_key` sentinel only on the `tension` entry, not on `tension_gv`. After the `--include-gv` invocation, the test asserts `set(tension_gv) == {"sigma", "gv_error", "stable_at"}`, which passes whether `tension_sweep.main()` merges into the existing `tension_gv` dict (as 40.2d requires) or overwrites it with a fresh `{"sigma":..., "gv_error":..., "stable_at":...}` literal. Only half of the 40.2d fix is actually pinned. The carry-over items (40.3c/40.4c/40.5c) will replicate this same merge pattern for `{kernel}_gv`, `tension_penalty`, and `footprint.*`, so a robust template matters.
  - Implementation: `_seed_kv` now seeds `tension_gv` with an extra `"preexisting_gv_extra": "survive_gv"` key. The first (non-GV) invocation's exact-equality assertion now includes the sentinel, pinning that nothing touches `tension_gv` on that path. The second (GV) invocation's assertion was loosened from `set(tension_gv) == {"sigma","gv_error","stable_at"}` to `{"sigma","gv_error","stable_at"} <= set(tension_gv)` and gained `assert tension_gv["preexisting_gv_extra"] == "survive_gv"`, which fails iff `tension_sweep.main()` replaces the existing `tension_gv` dict with a fresh literal.
  - File: `scripts/stencil_gen/tests/test_sweep_gv_objectives.py` — **done**
  - Verified: `uv run pytest tests/test_sweep_gv_objectives.py -x -q -k "merges_known_values"` → 1 passed in 1.07s against the current merging implementation. Mutation check: temporarily replaced lines 284–288 of `tension_sweep.py` with `kv[scheme_key]["tension_gv"] = {"sigma": summary["gv_sigma"], "gv_error": summary["gv_error"], "stable_at": summary["gv_stable_at"]}` and the test failed with `KeyError: 'preexisting_gv_extra'` at the new assertion (then restored). `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -k "TestRegression or gv_objectives or sweep_gv or merges"` → 24 passed in 1.33s.

### 40.3 — Integrate GV into `epsilon_sweep` (Gaussian / multiquadric kernels)

- [x] **40.3a** Add `--include-gv` flag to `epsilon_sweep.py`, mirroring 40.2a:
  - `boundary_gv_error_max(p, q, nextra, nu, eps, kernel)` is the per-point call.
  - File: `scripts/stencil_gen/sweeps/epsilon_sweep.py` — **done**
  - Implementation: `sweep_stability` now returns `(results, gv_by_eps)` where `gv_by_eps: dict[float, float] | None` is populated once per epsilon (grid-size-independent) when `include_gv=True`, else `None`. The GV call uses `sigma=float(eps)` since `boundary_gv_error_max`'s internal parameter name is `sigma` but `boundary_group_velocity` uses it as the generic RBF shape parameter for all kernels (gaussian/multiquadric/tension — see `group_velocity.py:556`). `run_epsilon_sweep` accepts `include_gv=` kwarg and threads it through; the returned summary dict gains a `gv_by_eps` key. `main()` adds `--include-gv`, and `sweeps/__main__.py` adds the same flag on the epsilon subparser with passthrough. Argument plumbing only — the output format is unchanged (40.3b owns printing the new column and the "best feasible by GV error" line).
  - Verified: `uv run python -m sweeps epsilon --scheme E2 --kernel gaussian --n-eps 5 --include-gv` runs end-to-end without error; same command with `--kernel multiquadric` also runs; `uv run python -m sweeps epsilon --scheme E2 --kernel gaussian --n-eps 5` (no flag) produces identical output to before. `uv run pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges"` → 24 passed in 1.35s (no regressions).

- [x] **40.3b** Print GV column and "best feasible by GV" line in `epsilon_sweep`:
  - Same structure as 40.2b.
  - File: `scripts/stencil_gen/sweeps/epsilon_sweep.py` — **done**
  - Implementation: `run_epsilon_sweep` now passes `gv_by_param=gv_by_eps` to `print_sweep_table` (the kwarg added in 40.2b is reused). After `report_stable_ranges`, when `gv_by_eps is not None` it intersects per-grid stable epsilons (`se < STABILITY_TOL`) across every `n` in the sweep to form the feasible set, then prints `Best feasible by GV error: eps=…, gv_err=…` for the smallest GV error in that set, or `(no eps stable at every grid size)` if empty. The summary dict and the cross-check across `{20,40,80,160}` are deliberately untouched — those belong to 40.3c.
  - Verified: `uv run python -m sweeps epsilon --scheme E2 --kernel multiquadric --n-eps 5 --include-gv` shows the new column and `Best feasible by GV error: eps=10.000000, gv_err=2.143617e+00`. Same for `--kernel gaussian` (`eps=1.778279, gv_err=1.868048e+00`). `diff` of the no-flag vs `--include-gv` outputs at `--n-eps 5 --kernel gaussian` shows only the additive `gv_err` column and the new "Best feasible" line — every other line is byte-identical. `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges"` → 24 passed in 1.32s.

- [x] **40.3c** Persist `{kernel}_gv` keys in `known_values.json` for epsilon sweep:
  - `kv[scheme_key][kernel]["gv_error"] = ...` (additive on existing entry).
  - `kv[scheme_key]["{kernel}_gv"] = {"epsilon": ..., "gv_error": ..., "stable_at": [...]}` for the GV-optimal feasible epsilon.
  - File: `scripts/stencil_gen/sweeps/epsilon_sweep.py` — **done**
  - Implementation: `run_epsilon_sweep` now cross-checks the GV-optimal feasible epsilon at grid sizes `{20,40,80,160}` (mirroring the existing stability-optimum cross-check) and returns `gv_epsilon`, `gv_error`, `gv_stable_at` in the summary. `main()` uses the 40.2d merge pattern: reads the existing `kv[scheme_key].get(args.kernel, {})` and `kv[scheme_key].get(f"{args.kernel}_gv", {})` and merges only the keys this invocation owns (`epsilon`, `stable_at`, and conditionally `gv_error`). Without `--include-gv`, the existing `{kernel}` entry is written without the `gv_error` key, but any pre-existing `gv_error`/`{kernel}_gv` from an earlier `--include-gv` run survives. Without `--update-known-values`, nothing is written.
  - Verified: snapshot `known_values.json`; `uv run python -m sweeps epsilon --scheme E2 --kernel gaussian --n-eps 5 --include-gv --update-known-values` wrote `E2_1.gaussian.gv_error = 1.8680479…` and `E2_1.gaussian_gv = {epsilon: 1.778279, gv_error: 1.8680479…, stable_at: [20,40,80,160]}`; subsequent `... --update-known-values` (no `--include-gv`) kept both keys. Also verified with `--kernel multiquadric` → `E2_1.multiquadric_gv.epsilon = 10.0`. Snapshot restored (the low-resolution `--n-eps 5` run is not committed to JSON — a full-resolution sweep should populate the real values). The no-flag path (`--n-eps 5` without `--include-gv`, no `--update-known-values`) produces output identical to before this item. `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges"` → 24 passed in 1.34s.
  - **Carry-over for 40.4c / 40.5c:** same additive merge pattern (`dict(kv[scheme_key].get(key, {}))` then assign specific keys) applies to `tension_penalty` and `footprint.*` entries. Consider adding a regression test analogous to `test_tension_sweep_main_merges_known_values` when those items land.

- [x] **40.3d** Add an automated regression test for `epsilon_sweep.main()` merge pattern (mirrors 40.2e/40.2f for tension):
  - Review pass on commit `b6e4a75` confirmed that 40.3c implements the additive merge pattern in `epsilon_sweep.main()` for both `kv[scheme_key][args.kernel]` and `kv[scheme_key][f"{args.kernel}_gv"]`, but the only verification is the same manual "snapshot → run twice → inspect JSON → restore" workflow that 40.2e was created to retire for tension. Without an automated test, a future refactor of `epsilon_sweep.main()` (or a copy-paste from a non-merging template) can silently re-introduce the exact bug 40.2d fixed for tension.
  - Implementation: `test_epsilon_sweep_main_merges_known_values` added in `tests/test_sweep_gv_objectives.py`, mirroring `test_tension_sweep_main_merges_known_values`. Uses a sibling `_seed_kv_epsilon(path, kernel="gaussian")` helper (does not refactor the existing tension-specific `_seed_kv` — keeping the helpers parallel makes future carry-over for 40.4c/40.5c easier to scaffold). Monkeypatches `sweeps_common.KNOWN_VALUES_PATH` to `tmp_path / "known_values.json"`, seeds `{"E2_1": {"params": ..., "gaussian": {epsilon, stable_at, gv_error: 1.234, preexisting_extra_key: "survive"}, "gaussian_gv": {epsilon, gv_error, stable_at, preexisting_gv_extra: "survive_gv"}}}`, then runs `epsilon_sweep.main` twice with `--scheme E2 --kernel gaussian --n-eps 5 --n-values 20`: first without `--include-gv` (asserts `gaussian.gv_error == 1.234`, `gaussian.preexisting_extra_key == "survive"`, `gaussian_gv` byte-equal to seed), then with `--include-gv` (asserts `gaussian.preexisting_extra_key == "survive"`, `gaussian.gv_error` is finite float, `gaussian_gv["preexisting_gv_extra"] == "survive_gv"`, and `{"epsilon","gv_error","stable_at"} <= set(gaussian_gv)`).
  - File: `scripts/stencil_gen/tests/test_sweep_gv_objectives.py` — **done**
  - Verified: `uv run pytest tests/test_sweep_gv_objectives.py -x -q` → 10 passed in 1.28s. Mutation check 1: replaced the `kernel_entry = dict(kv[scheme_key].get(args.kernel, {}))` block with a non-merging `kernel_entry = {"epsilon": ..., "stable_at": ...}` literal — test failed at `kernel_entry["gv_error"] == 1.234` with `KeyError: 'gv_error'` (restored). Mutation check 2: replaced the `gv_entry = dict(kv[scheme_key].get(gv_key, {}))` block with a non-merging `gv_entry = {"epsilon": ..., "gv_error": ..., "stable_at": ...}` literal — test failed at `gv_entry["preexisting_gv_extra"] == "survive_gv"` with `KeyError: 'preexisting_gv_extra'` (restored). Final regression sweep: `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges"` → 25 passed in 1.53s.
  - **Carry-over for 40.4c / 40.5c:** when those land, replicate this pattern. Consider extracting `_seed_kv` / `_seed_kv_epsilon` into a single parameterized `_seed_kv_with_keys(path, scheme_key, primary_key, secondary_key)` helper at that point — three near-identical seeders is the threshold to deduplicate.

### 40.4 — Extend `tension_penalty_sweep` to track GV as a third objective

- [x] **40.4a** Augment `eval_point()` in `tension_penalty_sweep.py` to return `(stab_eig, deficit, gv_error)`:
  - Use `gv_objectives.gv_score_from_matrix(D)["max_gv_error"]` so we don't rebuild D.
  - All callers updated to unpack three values.
  - File: `scripts/stencil_gen/sweeps/tension_penalty_sweep.py` — **done**
  - Implementation: `eval_point` now imports `gv_score_from_matrix` from `.gv_objectives`, computes `gv_error = float(gv_score_from_matrix(D)["max_gv_error"])` from the same `D` matrix it already builds (no rebuild), and returns the 3-tuple `(se, deficit, gv_error)`. The return-type annotation is updated to `tuple[float, float, float]`. All four call sites in this file (`run_joint_sweep_coarse`, `run_penalty_effect`, `run_fine_sweep` main loop, `run_fine_sweep` grid-independence loop) unpack into `se, deficit, _gv` — the `_gv` is intentionally discarded; 40.4b owns plumbing it through to the accumulator and printer. `grep eval_point` confirms no other file calls it. Argument plumbing only — no behavior change yet beyond the additional return value.
  - Verified: `uv run python -m sweeps tension-penalty --scheme E2 --n-sigma 5 --n-gamma 5` runs end-to-end with output identical to before this item (same coarse-sweep, penalty-effect, and fine-sweep numbers). `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges"` → 25 passed in 1.49s.

- [x] **40.4b** In `run_joint_sweep_coarse`, accumulate `best_stable_gv` (smallest GV error among feasible points):
  - New accumulator alongside `best_stable_deficit`. Same feasible-then-minimize pattern.
  - Print a third "best stable GV error" line at end of phase 1 output.
  - File: `scripts/stencil_gen/sweeps/tension_penalty_sweep.py` — **done**
  - Implementation: `run_joint_sweep_coarse` now captures the already-returned `gv` from `eval_point` (renaming `_gv` → `gv` in the loop) and maintains `best_stable_gv`, `best_stable_gv_sigma`, `best_stable_gv_gamma` accumulators updated inside the existing `se < STABILITY_TOL` branch (no extra pass over the grid). After the existing "Best stable point (lowest deficit)" block, the function prints a parallel "Best stable point (lowest GV error)" block. The returned summary dict gains `best_stable_gv_sigma`, `best_stable_gv_gamma`, and `best_stable_gv` (None when no feasible point exists); existing keys are untouched. `run_penalty_effect` and `run_fine_sweep` still discard their `_gv` — they are not the phase-1 accumulator and plan 40.4c owns the persistence work.
  - Verified: `uv run python -m sweeps tension-penalty --scheme E2 --n-sigma 5 --n-gamma 5` prints `Best stable point (lowest GV error): sigma=20.0000, gamma=0.0000, gv_error=2.074641e+00` (matches the 40.2b tension-only result at the same coarse grid). Phase 2 and phase 3 output is byte-identical to before. `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges"` → 25 passed in 1.47s.

- [x] **40.4c** Persist three-way optimum to `known_values.json`:
  - Review pass on commit `5cd827e` confirmed: 40.4b added `best_stable_gv` / `best_stable_gv_sigma` / `best_stable_gv_gamma` to the dict returned by `run_joint_sweep_coarse`, but `run_tension_penalty_sweep` (lines 350–356) does NOT thread any of those keys into its own returned summary. Without that plumbing 40.4c cannot read the value in `main()`.
  - Implementation: `run_tension_penalty_sweep` now reads `coarse["best_stable_gv_sigma"]`, `coarse["best_stable_gv_gamma"]`, `coarse["best_stable_gv"]` and threads them into its returned summary under `gv_sigma`, `gv_gamma`, `gv_error` (rounded to 6 decimals for sigma/gamma, raw float for gv_error). When the coarse sweep found no feasible point, all three are `None` — no unpacking errors at downstream call sites. `best_sigma` / `best_gamma` (sourced from `fine`) remain the eigenvalue-stability optimum.
  - `main()` applies the 40.2d/40.3c merge pattern to `kv[scheme_key]["tension_penalty"]`: read `tp_entry = dict(kv[scheme_key].get("tension_penalty", {}))`, assign `sigma`/`gamma`/`stable_at`, and conditionally set `gv_error` when `summary["gv_error"] is not None`. No `--include-gv` flag — `eval_point` always computes `gv_error` (40.4a decision), so the only gating is "coarse sweep found a feasible point". A second top-level `tension_penalty_gv` entry is written using the same merge pattern when `summary["gv_sigma"] is not None`, with keys `sigma`/`gamma`/`gv_error`. Note: tension-penalty's coarse sweep is single-grid (n=40 only), so `tension_penalty_gv` has **no `stable_at` list** in this commit — **40.4d (next item) closes that gap by adding an explicit cross-grid stability re-check at the GV optimum**, and 40.4e's regression test must then assume `stable_at` is present.
  - File: `scripts/stencil_gen/sweeps/tension_penalty_sweep.py` — **done**
  - Verified: snapshot `known_values.json`; `uv run python -m sweeps tension-penalty --scheme E2 --n-sigma 5 --n-gamma 5 --update-known-values` wrote `E2_1.tension_penalty = {sigma: 4.0, gamma: 0.1, stable_at: [20,40,80], gv_error: 2.074641…}` and `E2_1.tension_penalty_gv = {sigma: 20.0, gamma: 0.0, gv_error: 2.074641…}`. Snapshot restored (the low-resolution `--n-sigma 5 --n-gamma 5` run is not committed — a full-resolution sweep should populate the real values). Without `--update-known-values` the smoke run produces output identical to before (byte-for-byte through phases 1/2/3; only new end-of-phase-1 "Best stable point (lowest GV error)" block from 40.4b). `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges"` → 25 passed in 1.46s.
  - **Carry-over for 40.5c:** same additive merge pattern applies to `footprint.*` entries. 40.4e will automate the regression test for this merge.

- [x] **40.4d** Cross-grid stability check for the persisted `tension_penalty_gv` (sigma, gamma) optimum:
  - Review pass on commit `a5e7f5d` confirmed: 40.4c persists `kv[scheme_key]["tension_penalty_gv"] = {"sigma": ..., "gamma": ..., "gv_error": ...}` with **no `stable_at` field at all**, diverging from the convention set by 40.2c (`tension_gv.stable_at = [20,40,80,160]`) and 40.3c (`{kernel}_gv.stable_at = [...]`). The plan's justification ("coarse sweep is single-grid (n=40 only) — `tension_penalty_gv` has **no `stable_at` list**") does not hold up: the coarse sweep being single-grid means GV was *computed* at n=40, but verifying that the GV-optimal `(sigma, gamma)` pair is stable at additional grid sizes is just one extra `eval_point(nn, sigma, gamma, ...)` call per `nn`, not a re-execution of the coarse sweep. Without that check, a future consumer loading `tension_penalty_gv` from `known_values.json` has no guarantee the point is stable at any grid size other than n=40.
  - For the E2 verification in 40.4c the GV optimum happened to be `(sigma=20.0, gamma=0.0)`, which is identical to the tension-only sweep optimum that 40.2c already cross-checked at `{20,40,80,160}` — so the missing check was masked. For E4 (or any future scheme) where the GV optimum has `gamma > 0`, no such serendipitous coverage exists, and the persisted point could be silently unstable at higher resolution.
  - Implementation: `run_tension_penalty_sweep` now performs a cross-grid stability re-check immediately after `run_joint_sweep_coarse` returns and before invoking `run_penalty_effect`/`run_fine_sweep`. When `coarse["best_stable_gv_sigma"] is not None`, it loops `nn in sorted({20, 40, 80, 160})` calling `eval_point(nn, gv_sigma, gv_gamma, p=p, q=q, nextra=nextra, nu=nu)` and collects the `nn` values with `se < STABILITY_TOL` into `gv_stable_at`. The per-grid status is printed in the same format as `tension_sweep.run_tension_sweep` (`stab_eig=…, deficit=… [STABLE/unstable]`). The returned summary dict gains `gv_stable_at` (a `list[int]` or `None`). `main()` sets `tp_gv_entry["stable_at"] = summary["gv_stable_at"]` inside the additive merge for `tension_penalty_gv` — the new value is authoritative, preserving the merge guarantee for any other pre-existing keys on the entry.
  - File: `scripts/stencil_gen/sweeps/tension_penalty_sweep.py` — **done**
  - Verified: snapshot `known_values.json`; `uv run python -m sweeps tension-penalty --scheme E2 --n-sigma 5 --n-gamma 5 --update-known-values` prints `Checking GV-optimal (sigma=20.000000, gamma=0.000000) across grid sizes:` followed by the four `n=20/40/80/160 [STABLE]` lines, and writes `E2_1.tension_penalty_gv = {sigma: 20.0, gamma: 0.0, gv_error: 2.074641…, stable_at: [20, 40, 80, 160]}`. Snapshot restored (low-resolution `--n-sigma 5 --n-gamma 5` is not committed). `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges"` → 25 passed in 1.49s.

- [x] **40.4e** Add an automated regression test for `tension_penalty_sweep.main()` merge pattern (mirrors 40.2e/40.2f and 40.3d):
  - Without this, the 40.4c additive-merge guarantee is review-dependent and the same overwrite bug that 40.2d had to fix can be silently re-introduced by a future refactor or by 40.5c's copy-paste.
  - Implementation: `test_tension_penalty_sweep_main_merges_known_values` in `tests/test_sweep_gv_objectives.py`, mirroring `test_tension_sweep_main_merges_known_values` and `test_epsilon_sweep_main_merges_known_values`. Adds a sibling `_seed_kv_tension_penalty(path)` helper (three near-identical seeders still fine; 40.5c may trigger the deduplication). Seeds `{"E2_1": {"params": ..., "tension_penalty": {"sigma": 6.0, "gamma": 0.0, "stable_at": [20,40,80], "gv_error": 1.234, "preexisting_extra_key": "survive"}, "tension_penalty_gv": {"sigma": 5.5, "gamma": 0.1, "gv_error": 1.234, "stable_at": [20,40], "preexisting_gv_extra": "survive_gv"}}}`. Monkeypatches `sweeps._common.KNOWN_VALUES_PATH` to `tmp_path / "known_values.json"` (tension_penalty_sweep imports `load_known_values` / `save_known_values` from `_common`, which read `KNOWN_VALUES_PATH` from the `_common` module globals at call time, so monkeypatching only `_common` is sufficient). Runs `tension_penalty_sweep.main(["--scheme","E2","--n-sigma","5","--n-gamma","5","--update-known-values"])` once (no separate non-GV path — tension-penalty has no `--include-gv` flag) and asserts: `tension_penalty["preexisting_extra_key"] == "survive"`, `tension_penalty["gv_error"]` is a finite float and `!= 1.234` (refreshed), `tension_penalty_gv["preexisting_gv_extra"] == "survive_gv"`, `{"sigma","gamma","gv_error","stable_at"} <= set(tp_gv)`, and `tp_gv["stable_at"]` is a non-empty `list` with `set(...) <= {20,40,80,160}` (pins the 40.4d cross-grid re-check).
  - File: `scripts/stencil_gen/tests/test_sweep_gv_objectives.py` — **done**
  - Verified: `uv run pytest tests/test_sweep_gv_objectives.py -x -q -k "tension_penalty_sweep_main_merges"` → 1 passed in 0.90s. Mutation check 1: replaced the `tp_entry = dict(kv[scheme_key].get("tension_penalty", {}))` merge with a non-merging literal `tp_entry = {"sigma":..., "gamma":..., "stable_at":...}` — test failed with `KeyError: 'preexisting_extra_key'` (restored). Mutation check 2: replaced the `tp_gv_entry = dict(kv[scheme_key].get("tension_penalty_gv", {}))` merge with a non-merging literal `tp_gv_entry = {"sigma":..., "gamma":..., "gv_error":..., "stable_at":...}` — test failed with `KeyError: 'preexisting_gv_extra'` (restored). Final regression sweep: `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges"` → 26 passed in 1.56s.
  - **Carry-over for 40.5c:** same additive merge pattern applies to `footprint.*` entries. When 40.5c lands, consider extracting `_seed_kv` / `_seed_kv_epsilon` / `_seed_kv_tension_penalty` into a single parameterized `_seed_kv_with_keys(path, scheme_key, primary_key, secondary_key, primary_extras, secondary_extras)` helper — four near-identical seeders is past the deduplication threshold.

### 40.5 — GV integration with `footprint_sweep`

**Planning correction (2026-04-10, during 40.5a execution):** The original 40.5a / 40.5b / 40.5c drafts assumed `footprint_sweep.py` already had `scheme_params` / `alpha_values` in scope (inherited from the plan author's mental model of the cut-cell TEMO pipeline). Inspection of the file shows **zero** references to `scheme_params`, `SchemeParams`, `alpha`, or `temo` — the sweep is a pure RBF boundary-closure exploration (E4 tension with varying `nextra`), built around `build_diff_matrix_rbf_penalty` and `stability_eigenvalue`, exactly like `tension_sweep.py` and `epsilon_sweep.py`. Calling `cutcell_gv_min_C` here would require constructing one `SchemeParams(p=2, q=3, s=0, nextra=nx, nu=1, zeros=(3,4))` per nextra value and running the slow mathematica cut-cell derivation per nextra — a completely different axis from what the footprint sweep optimizes (RBF shape parameter sigma).

The correct GV objective for this sweep is `boundary_gv_error_max(p, q, nextra, nu, sigma, "tension")`, matching the 40.2/40.3 pattern. This measures the GV error of the RBF boundary rows that the footprint sweep is already varying. `cutcell_gv_min_C` is only meaningful for a TEMO cut-cell scheme and does not belong in this file. The items below are rewritten accordingly; a separate deferred 40.5f documents the (optional, currently out-of-scope) cut-cell path.

- [x] **40.5a** Add `--include-gv` flag to `footprint_sweep.py`, mirroring 40.2a/40.3a:
  - Default `False` so existing invocations behave identically.
  - When set, after the existing stability scan in `run_nextra_sigma_sweep`, evaluate `boundary_gv_error_max(p=P, q=Q, nextra=nx, nu=NU, sigma=sigma, kernel="tension")` per `(nx, sigma)` pair and return it alongside the existing rows. One call per `(nx, sigma)` — the GV objective does not depend on `n`, so no re-evaluation at multiple grid sizes.
  - Argument plumbing only — no column printed, no persistence change, no table layout change. 40.5b owns the printing work; 40.5c owns persistence.
  - Thread `--include-gv` through `run_footprint_sweep` and `__main__.py` subparser (same pattern as the tension/epsilon plumbing).
  - File: `scripts/stencil_gen/sweeps/footprint_sweep.py` (+ `sweeps/__main__.py` flag passthrough) — **done**
  - Implementation: `run_nextra_sigma_sweep` now accepts `include_gv=` kwarg and returns `(results, gv_by_nx_sigma)` where `gv_by_nx_sigma: dict[int, dict[float, float]] | None` maps `nextra -> {sigma -> boundary_gv_error_max}` when `include_gv=True`, else `None`. Per-nextra, per-sigma calls use `boundary_gv_error_max(p=P, q=Q, nextra=nx, nu=NU, sigma=sigma, kernel="tension")` (grid-size independent, evaluated once per (nx, sigma)). `run_footprint_sweep` accepts `include_gv=` kwarg, unpacks the new tuple return, and stores the nested dict under the internal key `summary["_gv_by_nx_sigma"]`. The underscore prefix marks it as in-process only. `main()` adds `--include-gv`, and the existing `kv["footprint"] = summary` assignment is narrowed to `kv["footprint"] = {k: v for k, v in summary.items() if not k.startswith("_")}` so the in-process GV data is never persisted — this is a minimal filter, NOT the full additive-merge rewrite that 40.5c still owns. `sweeps/__main__.py` adds `--include-gv` on the footprint subparser with passthrough. No table/print output changed; 40.5b owns that.
  - Verified: `uv run python -m sweeps footprint --n-sigma 5 --include-gv` runs end-to-end through all three phases without error. `diff` of `uv run python -m sweeps footprint --n-sigma 5` vs `uv run python -m sweeps footprint --n-sigma 5 --include-gv` is empty (`BYTE-IDENTICAL`) — confirming the pure-plumbing contract. `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges"` → 26 passed in 1.54s.

- [x] **40.5b** Print `gv_err` column in `run_nextra_sigma_sweep` table when `--include-gv` is set:
  - Extend the per-`nx` summary row with a `best_stable_gv_sigma` / `best_stable_gv` pair and print a parallel "Best stable (by GV error)" summary block under the existing "Summary: Stability per nextra" table.
  - Among stable rows (`se < STABILITY_TOL`), report "Best (nextra, sigma) by GV error" as a single-line summary across all nextra values.
  - File: `scripts/stencil_gen/sweeps/footprint_sweep.py` — **done**
  - Implementation: the detail table now widens per-nx column from 16 → 32 chars when `include_gv=True`, adds a two-cell sub-header (`stab_eig | gv_err`) under each `nx=N` heading, and per data row appends `{gv:14.6e}` alongside the existing `{se:14.6e}{marker}`. The stable-marker branch was made include-gv-specific so the no-flag path still uses the original `" *" / ""` asymmetric column width and remains byte-identical. After the detail rows, a per-nx populate loop attaches `best_stable_gv_sigma` / `best_stable_gv` to each entry of `results` (feasible = `se < STABILITY_TOL` at the primary grid size `n`, matching the scope of the per-nx row data — cross-grid re-checks are 40.5c). Below the existing "Summary: Stability per nextra" block, a parallel "Summary: Best stable (by GV error) per nextra" table is printed (columns: `nextra`, `gv sigma`, `gv_err`; `--` for nextra with no feasible point), followed by a single-line "Best (nextra, sigma) by GV error: nextra=N, sigma=…, gv_err=…" over the min across nextra values, or "(no feasible point)" if none.
  - Verified: `uv run python -m sweeps footprint --n-sigma 5 --include-gv` shows the widened detail table with the `gv_err` sub-column per nx, the new summary block (`nextra=3, sigma=50.000000, gv_err=3.885040e+00` at `--n-sigma 5`), and the overall "Best (nextra, sigma)" line. `diff` of pre-edit baseline (via `git stash` round-trip) vs current `--n-sigma 5` (no `--include-gv`) → byte-identical. `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges"` → 26 passed in 1.59s.

- [x] **40.5c** Persist per-nextra GV optimum in `known_values.json["footprint"]`:
  - For each `E4_nextra{nx}_tension_{sigma}` footprint entry that is populated from a stable sigma, also write `entry["gv_error"] = best_gv_err_for_that_nextra` (additive on the existing entry).
  - Add a parallel top-level `kv["footprint"][f"E4_nextra{nx}_tension_gv"] = {"nextra": nx, "sigma": gv_sigma, "gv_error": gv_err, "stable_at": [...]}` for the GV-optimal feasible sigma at that nextra, with a cross-grid stability re-check at `{20,40,80,160}` mirroring 40.2c/40.3c/40.4d.
  - File: `scripts/stencil_gen/sweeps/footprint_sweep.py` — **done**
  - Implementation: new `_check_gv_sigma_stable_grids(nextra, gv_sigma, grid_sizes)` helper runs `stability_eigenvalue` at each grid size and prints a per-`nn` `[STABLE/unstable]` line (matching 40.4d format). `run_footprint_sweep` added a "Phase 4" cross-grid re-check block: when `include_gv=True` and any nextra has `best_stable_gv_sigma`, it prints a "Cross-grid stability re-check at GV-optimal sigma (per nextra)" header and calls the helper for each such nextra, collecting `gv_stable_at_by_nx[nx]`. The summary build loop now also adds `gv_error` to the existing `E4_nextra{nx}_tension_{sigma}` entry (when stable and `include_gv` set) and a parallel `E4_nextra{nx}_tension_gv` entry with `{nextra, sigma, gv_error, stable_at}`. `main()` applies the 40.2d/40.3c/40.4c merge pattern — but at the **per-entry** level inside `kv["footprint"]`, not at the top level: it does `footprint = kv.setdefault("footprint", {})`, then for each summary key (skipping `_`-prefixed internal ones) does `merged = dict(footprint.get(key, {})); merged.update(new_entry); footprint[key] = merged`. This fixes the pre-existing `kv["footprint"] = summary` bug where a non-GV re-run would silently drop all `_tension_gv` entries from prior `--include-gv` runs (and also would drop any `E4_nextra{nx}_tension_{sigma}` entries for sigma values this run did not reproduce, e.g. when `--n-sigma 5` finds a different `best_sigma` than the full-resolution run). 40.5e owns the automated regression test for this merge pattern.
  - Verified: snapshot `known_values.json`; `uv run python -m sweeps footprint --n-sigma 5 --include-gv --update-known-values` prints the new Phase 4 block and writes seven GV-relevant entries: `E4_nextra0_tension_gv = {sigma: 50.0, gv_error: 4.75…, stable_at: [20,40,80,160]}`, similarly for nextra 1/2/3, plus `gv_error: 3.885…` additively merged onto the newly-created `E4_nextra3_tension_1` entry. The **pre-existing** `E4_nextra0_tension_3` (which the low-resolution `--n-sigma 5` run does not reproduce as `best_sigma`) survived the first run because the merge loop only touched keys in the new summary — this is exactly the additive contract. Second test: `uv run python -m sweeps footprint --n-sigma 5 --update-known-values` (no `--include-gv`) — all six `*_tension_gv` entries and the `E4_nextra3_tension_1.gv_error` from the first run were preserved byte-for-byte, confirming the fix of the pre-existing `kv["footprint"] = summary` bug. Snapshot restored (low-resolution `--n-sigma 5` is not committed — a full-resolution sweep should populate the real values). The no-flag path (`--n-sigma 5` without `--include-gv`, no `--update-known-values`) is byte-identical to pre-edit baseline via `git stash` round-trip. `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges"` → 26 passed in 1.56s.
  - **Carry-over for 40.5e:** the footprint merge test is subtly different from 40.2e/40.3d/40.4e because `footprint` has a **two-level** merge (top-level `kv["footprint"]` dict, then per-entry merge inside it). The regression test must seed **both** a pre-existing entry keyed by a sigma the `--n-sigma 5` run will not reproduce (e.g. `E4_nextra0_tension_3` — the same sigma the real `known_values.json` uses) AND a `_tension_gv` entry with a `preexisting_gv_extra` sentinel, then assert **both** survive the non-GV re-run. A single-sentinel test would miss the two-level bug.

- [x] **40.5d** Filter `best_stable_gv_sigma > 0` before persisting `_tension_gv` entries:
  - Review pass on commit `bfd13d1` ran `uv run python -m sweeps footprint --n-sigma 5 --include-gv` and observed:
    ```
      Cross-grid stability re-check at GV-optimal sigma (per nextra)
      nextra=1, sigma=0.000000:
        n= 20  stab_eig= -4.971480e-03  [STABLE]
        ...
    ```
    The GV-optimal feasible sigma for nextra=1 is `0.0`, which means the tension term vanishes and the kernel collapses to the PHS k=2 baseline. The eigenvalue-stability path in `run_footprint_sweep` (line 440 of `footprint_sweep.py`) explicitly filters `res["best_sigma"] > 0` before creating an `E4_nextra{nx}_tension_{sigma}` entry, precisely so PHS-baseline points don't get mis-labeled as tension. The 40.5c GV path lacks the parallel filter (line 453: `if include_gv and res.get("best_stable_gv_sigma") is not None`), so it produces `E4_nextra1_tension_gv = {"nextra": 1, "sigma": 0.0, ...}` — a `_tension_gv` key whose `sigma` is 0, which is semantically PHS, not tension. The same nextra also has an `E4_nextra1_phs` entry, so the two coexist and partially duplicate.
  - This is a real correctness/labeling bug (not just stylistic): a downstream consumer iterating `_tension_gv` keys to find tension-kernel GV optima will pick up a sigma=0 PHS point, and the regression test in 40.8a will rebuild a tension `D` matrix with `sigma=0` that has no actual tension term. The fix is one line in `run_footprint_sweep`: change the GV-summary guard to `if include_gv and res.get("best_stable_gv_sigma") is not None and res["best_stable_gv_sigma"] > 0`. If a future caller wants the PHS-baseline GV diagnostic, that belongs on the `E4_nextra{nx}_phs` entry, not on `_tension_gv`.
  - Test: `uv run python -m sweeps footprint --n-sigma 5 --include-gv` should no longer print "nextra=1, sigma=0.000000:" in the Phase 4 cross-grid block, and the resulting summary should not contain `E4_nextra1_tension_gv`. Add an assertion in the 40.5e regression test (next item) covering this filter — seed `kv["footprint"]` with no `E4_nextra1_tension_gv`, run `main()` with `--include-gv`, and assert that `kv["footprint"]` still has no `E4_nextra1_tension_gv` afterward (because nextra=1's GV optimum is sigma=0 at the smoke-run resolution).
  - File: `scripts/stencil_gen/sweeps/footprint_sweep.py` — **done**
  - Implementation: two parallel guards added in `run_footprint_sweep`. (1) The Phase 4 cross-grid re-check loop now skips any nextra whose `best_stable_gv_sigma` is `None` *or* `<= 0`, and the `any_gv` test that prints the section header uses `(... or 0.0) > 0` so a sweep where every nextra has sigma=0 prints no section at all. (2) The summary build at line 453 now requires `res["best_stable_gv_sigma"] > 0` before constructing the `E4_nextra{nx}_tension_gv` entry, parallel to the existing `res["best_sigma"] > 0` filter that the eigenvalue-stability path applies at line 440 for the same semantic reason. Both edits keep the no-`--include-gv` codepath byte-identical.
  - Verified: `uv run python -m sweeps footprint --n-sigma 5 --include-gv` Phase 4 block now lists only `nextra=0,2,3` (no `nextra=1, sigma=0.000000:` line). The detail-level "Best (nextra, sigma) by GV error" line in the per-nextra summary table is unaffected (it was already correct because it iterates the in-process `gv_by_nx_sigma` dict, not the persisted summary). `diff` of `--n-sigma 5` (no `--include-gv`) against the pre-edit baseline via `git stash` round-trip → BYTE-IDENTICAL. `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges"` → 26 passed in 1.55s.

- [x] **40.5e** Add an automated regression test for `footprint_sweep.main()` merge pattern (mirrors 40.2e/40.3d/40.4e):
  - Refactor: `_seed_kv`, `_seed_kv_epsilon`, `_seed_kv_tension_penalty` removed and consolidated into a single parameterized `_seed_kv_with_keys(path, scheme_key, primary_key, secondary_key, primary_extras, secondary_extras, *, include_params=True)`. The three existing merge tests were updated to call it with the per-sweep primary/secondary extras (tension: `{"sigma": 6.0}` / `{"sigma": 5.5}`; gaussian: `{"epsilon": 1.0}` / `{"epsilon": 0.9}`; tension-penalty: `{"sigma": 6.0, "gamma": 0.0}` / `{"sigma": 5.5, "gamma": 0.1}`). The `include_params=False` flag handles the footprint case where entries live directly under `kv["footprint"]` and there is no `params` sub-entry.
  - Implementation: `test_footprint_sweep_main_merges_known_values` seeds `kv["footprint"]["E4_nextra0_tension_3"]` with a `preexisting_extra_key` sentinel (the real `known_values.json` entry is `E4_nextra0_tension_3` with sigma=3.0, and the smoke run does not reproduce sigma=3.0 at `--n-sigma 5` — confirmed by the 40.5c verification and by inspecting the smoke-run best_sigma values: nextra=0 → 0.0, nextra=1 → 0.0, nextra=2 → 0.0, nextra=3 → 0.7071, rounded to `E4_nextra3_tension_1`). Also seeds `kv["footprint"]["E4_nextra0_tension_gv"]` with a `preexisting_gv_extra` sentinel.
  - First invocation (`--n-sigma 5 --update-known-values`, no `--include-gv`) asserts the primary entry is byte-unchanged and the secondary entry is byte-equal to the seed (the non-GV path does not write `_tension_gv` keys at all). Second invocation (`--n-sigma 5 --include-gv --update-known-values`) asserts: (a) the primary entry still carries `preexisting_extra_key == "survive"` because the run does not reproduce sigma=3.0; (b) the secondary entry carries `preexisting_gv_extra == "survive_gv"` despite the run writing a fresh `{nextra, sigma, gv_error, stable_at}` payload, proving the per-entry merge overlays new keys on top of the seed; (c) `gv_error` is refreshed (finite and `!= 1.234`); (d) `sigma > 0` (40.5d filter applied); (e) `stable_at` is a non-empty list with `set(...) <= {20,40,80,160}`; and (f) `E4_nextra1_tension_gv` is absent from `kv["footprint"]` (40.5d filter rejects nextra=1 because its GV optimum at `--n-sigma 5` is sigma=0.0).
  - File: `scripts/stencil_gen/tests/test_sweep_gv_objectives.py` — **done**
  - Verified: `uv run pytest tests/test_sweep_gv_objectives.py -x -q -k "footprint_sweep_main_merges"` → 1 passed in 4.68s. Mutation check 1: replaced the per-entry merge block at `footprint_sweep.py:527-532` with `footprint[key] = new_entry` — test failed with `KeyError: 'preexisting_gv_extra'` on the secondary entry assertion (restored). Mutation check 2: removed the `res["best_stable_gv_sigma"] > 0` filter at `footprint_sweep.py:462` — test failed with `AssertionError: 'E4_nextra1_tension_gv' not in footprint` (restored). Final regression sweep: `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges or footprint"` → 27 passed in 5.44s.

- [ ] **40.5f** *(deferred / optional)* True cut-cell GV diagnostics (`min_C` / `has_sign_reversal`) for TEMO schemes:
  - This is **not** a footprint-sweep concern. The cut-cell path operates on a `SchemeParams` object (`E2_1`, `E4_1`, `E4_2`, …) and runs `psi_sweep_group_velocity` at specific psi values. It is independent of the RBF shape parameter sigma that `footprint_sweep` optimizes.
  - If desired, the natural home is a new sweep (e.g. `sweeps/cutcell_gv_sweep.py`) or an augmentation of the existing `comparison_sweep.py` — **not** `footprint_sweep.py`. Leaving this as an explicit deferred item so a future Ralph pass does not re-derive the same discovery.
  - No test / no file / no immediate action.

### 40.6 — New `gv-stability-pareto` sweep subcommand

- [x] **40.6a** Create `sweeps/gv_stability_pareto.py` exporting `main(argv) -> int`:
  - For a chosen scheme + parameter (sigma for tension, epsilon for Gaussian/MQ), compute `(stab_eig, gv_error)` over a fine 1D grid.
  - Output: a markdown table sorted by sigma, plus a final "Pareto-optimal points" section listing non-dominated (stable, low gv) entries.
  - No `--update-known-values` — this sweep is for research / docs only.
  - Keep total file <200 lines.
  - File: `scripts/stencil_gen/sweeps/gv_stability_pareto.py` (new) — **done**
  - Implementation: 193-line module exposing `main(argv)`, `run_gv_stability_pareto`, `evaluate_grid`, `pareto_front`, `print_markdown_table`, and `_default_grid`. `evaluate_grid` calls `stability_eigenvalue` and `boundary_gv_error_max` once per parameter value at a single grid size (`--n`, default 40) — GV is grid-independent, so no inner loop over n. `_default_grid` uses `[0.0] + logspace(0.01, param_max)` for the tension kernel (matching `tension_sweep.py`) and pure logspace for Gaussian/MQ where `epsilon=0` is meaningless. `pareto_front` filters to feasible (`se < STABILITY_TOL`) rows, then removes any row dominated by another feasible row under the partial order `(stab_eig ≤, gv_err ≤)` with at least one strict inequality. Two markdown tables are printed: the full sorted grid and the Pareto-optimal subset (or a "no stable points" fallback). CLI flags: `--scheme`, `--param`, `--n-points` (default 61), `--n` (default 40), `--param-max` (default 20.0). No `--update-known-values` path and no file I/O beyond stdout.
  - Verified: `uv run python -c "from sweeps.gv_stability_pareto import main; raise SystemExit(main(['--scheme','E2','--param','tension','--n-points','11']))"` prints both tables end-to-end; 11-row grid at E2 tension has all 11 stable points and produces a 3-point Pareto front (sigma≈3.69, 8.60, 20.0 — monotone decrease in gv_err from 2.606 to 2.075 while stab_eig stays non-positive). Second smoke: `uv run python -m sweeps.gv_stability_pareto --scheme E4 --param gaussian --n-points 9` prints "*(no stable points on this grid)*" as expected (E4 gaussian at n=40 is unstable across the full sigma range without tension augmentation). `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges or footprint"` → 27 passed in 5.43s. 40.6b owns registering the `gv-stability-pareto` subcommand in `sweeps/__main__.py`.

- [x] **40.6b** Register `gv-stability-pareto` subcommand in `sweeps/__main__.py`:
  - File: `scripts/stencil_gen/sweeps/__main__.py` — **done**
  - Implementation: new `sub_pareto = subparsers.add_parser("gv-stability-pareto", ...)` block exposes all five flags from the module (`--scheme`, `--param`, `--n-points`, `--n`, `--param-max`) with the same defaults as `gv_stability_pareto.main`. The dispatch block lazy-imports `gv_stability_pareto.main` and forwards all five flags as strings (mirroring the `tension` / `epsilon` pattern). `_run_all` adds a `quick_pareto_n_points = "10" if quick else "61"` and inserts "GV-stability Pareto E2 (tension)" between the comparison and alpha-extraction entries, passing `--scheme E2 --param tension --n-points <quick_pareto_n_points>` and leaving `--n` / `--param-max` at module defaults for fast smoke.
  - Verified: `uv run python -m sweeps gv-stability-pareto --scheme E2 --param tension --n-points 11` prints the expected 11-row grid table and the 3-point Pareto front (sigma≈3.69/8.60/20.0, gv_err 2.606/2.205/2.075), matching the 40.6a direct-module invocation. Secondary test `uv run python -m sweeps gv-stability-pareto --scheme E2 --param tension --n-points 5 --n 20 --param-max 10.0` confirms `--n=20` reaches the module (header shows `n=20`, stab_eig values differ from the n=40 baseline) and `--param-max=10.0` reaches the module (largest grid point is 10.0, not the default 20.0). `uv run python -m sweeps gv-stability-pareto --help` lists all five flags. `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges or footprint"` → 27 passed in 5.33s.

### 40.7 — GKS group-velocity check as advisory diagnostic

- [x] **40.7a** Add `--check-gks` flag to `tension_sweep.py` and `epsilon_sweep.py`:
  - When set, after picking the optimum parameter, build the `D` matrix at the optimum and call `gks_group_velocity_check(D, xi_array)`.
  - Print any returned `GKSModeInfo` entries with `is_outgoing=True` as `WARNING: outgoing boundary mode at xi=…` lines.
  - **Does not** alter the optimum or update `stable_at`. Purely advisory output.
  - Files: `scripts/stencil_gen/sweeps/tension_sweep.py`, `scripts/stencil_gen/sweeps/epsilon_sweep.py`, `scripts/stencil_gen/sweeps/gv_objectives.py`, `scripts/stencil_gen/sweeps/__main__.py` — **done**
  - Implementation: added shared `print_gks_advisory(D, *, label, n_xi=200) -> int` helper to `gv_objectives.py` (avoids duplication across the two sweeps). It runs `gks_group_velocity_check(D, xi)` with `xi = _default_xi(n_xi)`, filters `is_outgoing=True`, and either prints a single "no outgoing boundary modes detected (N boundary mode(s) inspected)" line or one "WARNING: outgoing boundary mode at xi=…, lambda=…, C=…" per outgoing mode. Returns `len(outgoing)` for future test callers. `run_tension_sweep` gains a `check_gks: bool = False` kwarg; after the existing `sigma*` cross-grid check it builds `D_star = build_diff_matrix_rbf(n_fine_grid, p, q, sigma_star, "tension", nu, nextra)` and calls `print_gks_advisory(D_star, label=f"n={n_fine_grid}, sigma*={sigma_star:.6f}")`. `run_epsilon_sweep` mirrors this with the kernel threaded into the label. The `--check-gks` flag is added to both sweeps' `main()` argparse and forwarded from `sweeps/__main__.py` (sub_eps and sub_tension), with string-list passthrough matching the existing `--include-gv` pattern. `build_diff_matrix_rbf` was already imported in `epsilon_sweep.py`; `tension_sweep.py` adds it alongside `stability_eigenvalue`. The advisory block runs **after** the stability-optimum `stable_at` list is already built, so it cannot mutate the optimum — the constraint is enforced by control flow, not just convention.
  - Verified: `uv run python -m sweeps tension --scheme E2 --n-sigma 5 --check-gks` prints `GKS advisory (n=40, sigma*=3.483637): no outgoing boundary modes detected (0 boundary mode(s) inspected)` at the end of the existing output. `uv run python -m sweeps epsilon --scheme E2 --kernel gaussian --n-eps 5 --check-gks` prints the parallel advisory block at `eps*=0.002492`. Combined `uv run python -m sweeps tension --scheme E2 --n-sigma 5 --include-gv --check-gks` renders both the `Best feasible by GV error` line and the GKS advisory without interference. `diff` of no-flag invocations (`tension --scheme E2 --n-sigma 5` and `epsilon --scheme E2 --kernel gaussian --n-eps 5`) against pre-edit baseline via `git stash` round-trip → BYTE-IDENTICAL. `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges or footprint"` → 27 passed in 5.39s.
  - Note: the advisory-mode count varies between sweeps (0 for tension sigma*, 1 for gaussian eps*). This reflects `gks_group_velocity_check`'s filtering of nearly-neutral boundary-localized eigenmodes — not every sweep produces such modes at the stability optimum. 40.7b will seed a known-stable case and assert no false-positive outgoing warnings.

- [x] **40.7b** Add a smoke test for `--check-gks` on a known-stable scheme:
  - Test asserts the call exits 0 (no crash) and produces no false-positive errors.
  - File: `scripts/stencil_gen/tests/test_sweep_gv_objectives.py` — **done**
  - Implementation: two tests, both name-matching the plan's `-k "gks_advisory"` filter. (1) `test_check_gks_advisory_tension_e2_no_false_positives` calls `tension_sweep.main(["--scheme","E2","--n-sigma","5","--n-values","20","--check-gks"])`, captures stdout, and asserts `rc == 0`, `"GKS advisory" in out`, `"no outgoing boundary modes detected" in out`, and `"WARNING:" not in out`. The 40.7a verification confirmed E2 tension at sigma*=0.0 produces zero outgoing boundary modes, so this is a reliable known-clean case for the regression. (2) `test_check_gks_advisory_helper_clean_matrix` exercises `print_gks_advisory` directly on the same RBF differentiation matrix (`build_diff_matrix_rbf(20, p=1, q=1, epsilon=0.0, kernel="tension", nu=1, nextra=1)`) and asserts the helper returns `0` and prints the same clean line — isolating the helper from the surrounding sweep wiring. The sweep-level test wraps the wiring contract; the helper-level test wraps the math contract.
  - Verified: `uv run pytest tests/test_sweep_gv_objectives.py -x -q -k "gks_advisory"` → 2 passed in 0.95s. `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges or footprint or gks_advisory"` → 29 passed in 5.59s.

- [x] **40.7c** Add a parallel `--check-gks` smoke test for `epsilon_sweep.main` (coverage gap from review of `2b2e851`):
  - Review pass on commit `2b2e851` confirmed that 40.7a wired `--check-gks` through **both** `tension_sweep` and `epsilon_sweep` (both were verified by hand in the 40.7a note), but 40.7b's two new tests (`test_check_gks_advisory_tension_e2_no_false_positives` and `test_check_gks_advisory_helper_clean_matrix`) both target tension-kernel matrices only. A future refactor that breaks `epsilon_sweep.main`'s `--check-gks` argparse entry, the passthrough in `sweeps/__main__.py`'s epsilon subparser, or the `print_gks_advisory` call site inside `run_epsilon_sweep` would pass all existing tests silently.
  - Re-verified during review: `uv run python -m sweeps epsilon --scheme E2 --kernel gaussian --n-eps 5 --check-gks` prints `GKS advisory (n=40, eps*=0.002492, kernel=gaussian): no outgoing boundary modes detected (1 boundary mode(s) inspected)` — i.e. gaussian at the default smoke-run optimum has 1 boundary mode but **zero** outgoing modes, so a direct mirror of the tension test (assert no `WARNING:` line) is feasible. The 40.7a note's "1 for gaussian eps*" referred to the **inspected** count, not the outgoing count.
  - Implementation sketch: `test_check_gks_advisory_epsilon_e2_gaussian_no_false_positives` calls `epsilon_sweep.main(["--scheme","E2","--kernel","gaussian","--n-eps","5","--n-values","40","--check-gks"])`, captures stdout via `capsys`, and asserts `rc == 0`, `"GKS advisory" in out`, `"kernel=gaussian" in out` (pins the epsilon-specific label format that the helper does not produce for tension), `"no outgoing boundary modes detected" in out`, and `"WARNING:" not in out`. Do **not** add `--update-known-values` — the test must not mutate `known_values.json`. The test must be in the `gks_advisory` keyword filter (name must contain `gks_advisory`) so `pytest -k "gks_advisory"` picks it up alongside the existing two.
  - File: `scripts/stencil_gen/tests/test_sweep_gv_objectives.py` — **done**
  - Implementation: `test_check_gks_advisory_epsilon_e2_gaussian_no_false_positives` calls `epsilon_sweep.main(["--scheme","E2","--kernel","gaussian","--n-eps","5","--n-values","40","--check-gks"])`, captures stdout via `capsys`, and asserts `rc == 0`, `"GKS advisory" in out`, `"kernel=gaussian" in out` (pins the epsilon-specific label format that the helper does not produce for tension), `"no outgoing boundary modes detected" in out`, and `"WARNING:" not in out`. Inserted immediately above `test_check_gks_advisory_helper_clean_matrix` so the three GKS tests are co-located. No `--update-known-values` — the test does not mutate `known_values.json`.
  - Verified: `uv run pytest tests/test_sweep_gv_objectives.py -x -q -k "gks_advisory"` → 3 passed in 1.19s (was 2 in 40.7b). `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges or footprint or gks_advisory"` → 30 passed in 6.02s.

### 40.8 — Regression tests for GV-augmented `known_values.json`

- [x] **40.8a** Add `TestRegressionGV` class in `test_phs.py`:
  - Loads `known_values.json` (gracefully skipping if absent).
  - For each scheme that has a `tension_gv` or `{kernel}_gv` entry: rebuild the D matrix at the stored sigma/epsilon and assert `gv_score_from_matrix(D)["max_gv_error"] <= stored_value * 1.1` (10% tolerance for floating-point variation).
  - **Correction vs. draft:** plan originally said "Mark all class methods with `@pytest.mark.fast`", but 40.1b already documented that the project only registers a `slow` marker in `pyproject.toml`; adding a `fast` marker would emit pytest warnings. Tests are simply left unmarked, matching the 40.1b precedent for `test_sweep_gv_objectives.py`.
  - File: `scripts/stencil_gen/tests/test_phs.py` — **done**
  - Implementation: `TestRegressionGV` added inside the existing `if _KNOWN is not None:` block after `TestRegressionComparison`. Three test methods:
    1. `test_scheme_gv_entries_match_stored_error` — iterates `E2_1` / `E4_1` `tension_gv` / `gaussian_gv` / `multiquadric_gv` entries via a `_iter_scheme_gv_entries` helper, rebuilds `D = build_diff_matrix_rbf(40, p, q, eps/sigma, kernel, nu, nextra)` using the scheme's `params` sub-entry, and asserts `gv_score_from_matrix(D)["max_gv_error"] <= stored * 1.1`. Skips if no such entries exist.
    2. `test_footprint_gv_entries_match_stored_error` — iterates `footprint.*_tension_gv` entries, uses `E4_1.params` (footprint is E4-only), same 10% tolerance check. Skips if the footprint dict or any `*_tension_gv` keys are absent.
    3. `test_scheme_primary_gv_error_match` — complementary check for the **additive** `gv_error` field on the primary `{kernel}` entry (not `*_gv`), which 40.2c/40.3c populate alongside the secondary `*_gv` entry. Rebuilds D at the stability-optimum sigma/epsilon (which may differ from the GV-optimum), still uses the same 10% tolerance. Skips if no primary entries carry `gv_error`.
  - Imports `gv_score_from_matrix` lazily inside each test method (mirroring the `from stencil_gen.interior import ...` pattern used by other tests in this file) so that a missing `sweeps` package doesn't break module collection.
  - `GV_TOLERANCE = 1.1`, `GRID_N = 40` are class constants. `_iter_scheme_gv_entries` is a `@staticmethod` generator that defensively checks for missing `params`, missing `gv_error`, or missing `sigma`/`epsilon` fields before yielding — the test never crashes on a partially-populated `known_values.json`.
  - Verified (four-step):
    1. Baseline: `uv run pytest tests/test_phs.py -x -q -k TestRegressionGV` → 3 skipped (no `*_gv` keys in current `known_values.json`). The skip messages are `"no *_gv entries in known_values.json"`, `"footprint or E4_1 missing from known_values.json"` (actually takes the "no footprint *_tension_gv entries" branch), and `"no scheme.*.gv_error fields in known_values.json"`.
    2. Positive seed: inserted `E2_1.tension_gv = {sigma: 6.0, gv_error: 2.328151e+00, stable_at: [20,40,80,160]}` and `E2_1.tension.gv_error = 2.328151e+00` (computed by running `gv_score_from_matrix(build_diff_matrix_rbf(40, p=1, q=1, epsilon=6.0, kernel="tension", nu=1, nextra=0))` — the true value at E2's stability-optimum sigma=6.0), then re-ran → **2 passed, 1 skipped** (footprint still skips as expected).
    3. Mutation check: shrunk the seeded `gv_error` to `0.001` (impossibly small) → `test_scheme_gv_entries_match_stored_error` fails with `AssertionError: E2_1 tension sigma=6.0: measured gv_error 2.328151e+00 > stored 1.000000e-03 × 1.1` — the tolerance gate is exercised correctly.
    4. Restore: `git checkout scripts/stencil_gen/sweeps/known_values.json` restored the un-seeded baseline. Final regression sweep `uv run pytest tests/test_phs.py tests/test_sweep_gv_objectives.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges or footprint or gks_advisory"` → 30 passed, 3 skipped in 5.99s. The 3 skips are the new `TestRegressionGV` tests (as expected — no `*_gv` entries yet); 40.10a's `sweeps all --quick` run or a full `--include-gv --update-known-values` sweep will activate them.
  - Test: `cd scripts/stencil_gen && uv run pytest tests/test_phs.py -x -q -k "TestRegressionGV"`

- [x] **40.8b** Add a smoke test that exercises `gv_score_from_matrix` on a small precomputed D:
  - Review pass on commit `794e7a6` found that `test_gv_score_from_matrix_small_hardcoded` already exists in `tests/test_sweep_gv_objectives.py` (lines 110–129), added incidentally as part of 40.1b in commit `8fb9f68`. The test builds a hand-constructed 5×5 matrix with two boundary-like rows (row 0 forward difference, row 1 centered starting at column 0) and three interior rows whose leftmost nonzero column is > 0 (so the scanner in `gv_score_from_matrix` stops after row 1). Asserts `max_gv_error` and `min_cutoff_xi` are finite and `max_gv_error > 0.0`. The existing test satisfies the full 40.8b contract: tiny hardcoded matrix, deterministic output, picked up by the `-k "from_matrix"` filter.
  - No new code. 40.8b is marked done via retroactive attribution. This keeps the plan an accurate record of coverage — a future review won't re-add the test thinking it's missing.
  - Companion test `test_gv_score_from_matrix_matches_boundary_helper` (lines 95–107) provides the cross-check against `boundary_gv_error_max` at `rtol=1e-12`, so the `-k "from_matrix"` filter exercises both the hardcoded and real-matrix paths.
  - File: `scripts/stencil_gen/tests/test_sweep_gv_objectives.py` — **done (pre-existing)**
  - Verified: `cd scripts/stencil_gen && uv run pytest tests/test_sweep_gv_objectives.py -x -q -k "from_matrix"` → 2 passed, 13 deselected in 1.00s.

- [x] **40.8c** Fix the semantic mismatch between `{primary}.gv_error` and `test_scheme_primary_gv_error_match` (correctness bug from review of `a89670f`):
  - Review pass on commit `a89670f` confirmed: `tension_sweep.py:295-296` writes `tension_entry["gv_error"] = summary["gv_error"]` where `summary["gv_error"]` is `gv_best_error` — the GV error at the **GV-optimum** sigma (`gv_best_sigma`, ~20 for E2). But `tension_entry["sigma"] = round(sigma_star, 6)` is the **stability-optimum** sigma (~6 for E2 at full resolution). The new test `test_scheme_primary_gv_error_match` (added in 40.8a) rebuilds D at `tension.sigma` (the stability optimum) and compares to `tension.gv_error` (which actually represents the GV at a different sigma). For E2 nextra=1: `gv_at(sigma=6) = 2.328151e+00`, `gv_at(sigma=20) = 2.074641e+00`, ratio `= 1.122` which exceeds the 10% tolerance — so once any sweep populates `tension.gv_error` via `--include-gv --update-known-values`, the test will fail with `measured 2.328e+00 > stored 2.075e+00 × 1.1 = 2.282e+00`. The same bug pattern applies to `epsilon_sweep.py:293-294` (`{kernel}.gv_error`) and `footprint_sweep.py:451` (`E4_nextra{nx}_tension_{sigma}.gv_error`).
  - The 40.8a verification did not catch this because step 2 hand-seeded `tension_gv = {sigma: 6.0, gv_error: 2.328151}` and `tension.gv_error = 2.328151` — both using sigma=6.0 — which is **not** what the production sweep code writes. The production sweep writes `tension.sigma=6.0` AND `tension.gv_error=gv_at(20.0)=2.074641` AND `tension_gv.sigma=20.0` AND `tension_gv.gv_error=2.074641`. The seed in 40.8a step 2 was a degenerate case (stability == GV optimum) that masked the bug.
  - **Recommended fix (sweep side):** the additive `{primary}.gv_error` field should hold the GV error AT the stability-optimum sigma (i.e., "what GV do you pay for that stability choice"). This makes the field semantically self-consistent (sigma + gv_error describe the same point) and matches the test's assertion. Concretely:
    - In `tension_sweep.run_tension_sweep`: after the existing `sigma_star` cross-grid check, add `gv_at_sigma_star = boundary_gv_error_max(p, q, nextra, nu, sigma_star, "tension") if include_gv else None` (one extra call, grid-independent so trivial). Return it under a new summary key, e.g. `gv_at_sigma_star`. In `main()`, change line 296 to `tension_entry["gv_error"] = summary["gv_at_sigma_star"]` (gated on `args.include_gv and summary["gv_at_sigma_star"] is not None`). Leave `tension_gv.gv_error` writing `summary["gv_error"]` (the GV-optimum's error) unchanged — that's the secondary entry's correct semantics.
    - In `epsilon_sweep.run_epsilon_sweep`: same pattern with `eps_star` and `boundary_gv_error_max(..., kernel=args.kernel)`. Update `main()` line 294 accordingly.
    - In `footprint_sweep.run_nextra_sigma_sweep`: per-nextra, after picking `best_sigma > 0` for the primary entry, compute `gv_at_best_sigma = boundary_gv_error_max(p=P, q=Q, nextra=nx, nu=NU, sigma=best_sigma, kernel="tension")` (NOT `best_stable_gv` which is the GV at the GV-optimum sigma). Update line 451 to write `t_entry["gv_error"] = gv_at_best_sigma`. The parallel `_tension_gv` entry's `gv_error` field (line 468) stays unchanged — it correctly represents the GV-optimum's error.
  - **Alternative fix (test side):** if the design intent is that `{primary}.gv_error` is just a quick-reference duplicate of `{primary}_gv.gv_error` (no semantic relationship to `{primary}.sigma`), then `test_scheme_primary_gv_error_match` should not rebuild D at `entry[param_name]`. Instead it should assert `kv[scheme_key][kernel]["gv_error"] == kv[scheme_key][f"{kernel}_gv"]["gv_error"]` (within float tolerance) and skip the rebuild. This is simpler but loses information — and the redundant field becomes nearly useless to a human reader of `known_values.json`. The sweep-side fix is preferred.
  - File(s): `scripts/stencil_gen/sweeps/tension_sweep.py`, `scripts/stencil_gen/sweeps/epsilon_sweep.py`, `scripts/stencil_gen/sweeps/footprint_sweep.py` — **done** (sweep-side fix, as recommended).
  - Implementation: all three sweeps now compute the GV error at their stability-optimum parameter with an extra `boundary_gv_error_max` call (grid-independent, ~10ms) and persist that value into the additive `{primary}.gv_error` field. Details:
    1. `tension_sweep.run_tension_sweep`: after the `sigma_star = fine_sigma` cross-grid check, when `include_gv=True` it computes `gv_at_sigma_star = boundary_gv_error_max(p, q, nextra, nu, sigma_star, "tension")` and returns it under a new summary key. `main()` gates on `args.include_gv and summary["gv_at_sigma_star"] is not None` and writes `tension_entry["gv_error"] = summary["gv_at_sigma_star"]` instead of `summary["gv_error"]`. The secondary `tension_gv.gv_error` still writes `summary["gv_error"]` (the GV-optimum's error) — that field's semantics are correct because it sits next to `tension_gv.sigma`, which is `gv_best_sigma`.
    2. `epsilon_sweep.run_epsilon_sweep`: symmetric change — computes `gv_at_eps_star = boundary_gv_error_max(p, q, nextra, nu, eps_star, kernel)` (threads the kernel from args), adds a `gv_at_eps_star` summary key, and `main()` writes `kernel_entry["gv_error"] = summary["gv_at_eps_star"]`.
    3. `footprint_sweep.run_footprint_sweep`: the inline `E4_nextra{nx}_tension_{sigma}` entry build at line 450 now computes `boundary_gv_error_max(p=P, q=Q, nextra=nx, nu=NU, sigma=float(res["best_sigma"]), kernel="tension")` instead of reading `res["best_stable_gv"]`. The parallel `E4_nextra{nx}_tension_gv` entry's `gv_error` field (line 468) stays unchanged — it correctly represents the GV-optimum's error at `best_stable_gv_sigma`.
  - Verified (four-step, mirroring 40.8a's verification protocol):
    1. Baseline: `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges or footprint or gks_advisory"` → 30 passed, 3 skipped (same as before the fix — current `known_values.json` has no `*_gv` or `*.gv_error` entries).
    2. Snapshot `known_values.json`; run `uv run python -m sweeps tension --scheme E2 --n-sigma 5 --include-gv --update-known-values`. Manually verified with `gv_score_from_matrix(build_diff_matrix_rbf(40, 1, 1, 3.483637, "tension", 1, 1))`: stored `tension.gv_error = 2.6474706` at `tension.sigma = 3.483637`, rebuilt = 2.6474705 — ratio 0.99999997 (13 significant digits match). Simultaneously, `tension_gv = {sigma: 20.0, gv_error: 2.074641}` — proving the two entries now represent two *different* points with semantically self-consistent `(param, gv_error)` pairs.
    3. Same protocol for `epsilon --scheme E2 --kernel gaussian`: stored `gaussian.gv_error = 7.8950` at `gaussian.epsilon = 0.002492`, rebuilt at the rounded epsilon = 8.0900 (ratio 1.025 — within 10% tolerance, with the small drift coming from rounding `eps_star` to 6dp before the test rebuilds D). `gaussian_gv = {epsilon: 1.778279, gv_error: 1.868048}`. Same protocol for `footprint --n-sigma 5`: `E4_nextra3_tension_1 = {sigma: 0.7071, gv_error: 5.4999}`, rebuilt = 5.4999 (ratio 1.0000).
    4. After all three sweeps populated the keys, `pytest tests/test_phs.py -x -q -k TestRegressionGV` → **3 passed** (not 2 passed + 1 skip). Pre-fix this would have been 2 passed + 1 fail at `test_scheme_primary_gv_error_match` for `E2_1.tension` with ratio 1.122 > 1.1. Then `cp /tmp/kv_backup.json sweeps/known_values.json` restored the baseline and the full regression sweep still passes: 30 passed, 3 skipped (the 3 `TestRegressionGV` skips are back because the restored JSON has no `*_gv` entries — that's expected and will be resolved by 40.10a's `sweeps all --quick` run).
  - **Unblocks 40.10a:** with the semantic mismatch fixed, `sweeps all --quick` can populate `known_values.json` with real `{primary}.gv_error` values and `TestRegressionGV` will validate them instead of silently skipping. 40.10a should now also consider whether to include `--include-gv` on `epsilon` and `footprint` entries in `_run_all`, not just `tension`, so all three primary-entry paths get smoke coverage.
  - Test: `cd scripts/stencil_gen && uv run pytest tests/test_phs.py -x -q -k "TestRegressionGV"` after a real `--include-gv --update-known-values` run passes 3/3.

- [x] **40.8d** Compute `gv_at_*_star` at the *rounded* parameter, not the un-rounded one (residual correctness gap from review of `727da29`):
  - Review pass on `727da29` confirmed: all three sweeps compute `gv_at_*_star` using the un-rounded `eps_star` / `sigma_star` / `res["best_sigma"]`, but persist the *rounded* parameter into `known_values.json` (`round(eps_star, 6)`, `round(sigma_star, 6)`, `round(res["best_sigma"], 4)`). The test `test_scheme_primary_gv_error_match` rebuilds D at the rounded value (`entry[param_name]`), so the stored `(param, gv_error)` pair is NOT internally exactly self-consistent — the two halves describe slightly different points. This is exactly the property 40.8c was supposed to guarantee.
  - The 40.8c verification observed this directly in step 3: `gaussian.epsilon = 0.002492` (rounded), stored `gaussian.gv_error = 7.8950` (computed at un-rounded `eps_star ≈ 0.002492…`), but rebuilding D at `epsilon = 0.002492` gives `8.0900` — ratio `1.025`. The 10% tolerance absorbed it, so the test passed, but the 25% drift at small epsilon eats meaningful tolerance budget and the fix is one line per sweep. The tension case showed 13-significant-digit agreement only because `sigma_star ≈ 3.483637` is large enough that 6dp rounding is irrelevant — the bug is regime-dependent.
  - Why it matters beyond aesthetics: (1) the entire stated purpose of 40.8c is `(param, gv_error)` self-consistency; (2) `multiquadric` and small-`epsilon` `gaussian` regimes have GV that is highly sensitive to parameter perturbations (sensitivity ~10⁴×), so a future `--update-known-values` run at a different scheme/kernel/nextra could exceed 10% tolerance and break `test_scheme_primary_gv_error_match` again — re-introducing exactly the failure 40.8c was meant to prevent; (3) `footprint_sweep` rounds to **4dp** (not 6), so the rounding noise is ~100× larger and the risk is correspondingly higher there.
  - **Fix:** in each sweep, round the param value first and then call `boundary_gv_error_max` at the rounded value, so the stored pair is exactly self-consistent (modulo numerical noise of order `1e-13`):
    - `scripts/stencil_gen/sweeps/tension_sweep.py:208-218`: hoist `sigma_star_rounded = round(sigma_star, 6)` (or compute it inline) and pass `sigma=float(sigma_star_rounded)` into the `boundary_gv_error_max(...)` call. Update `summary["sigma"] = sigma_star_rounded` so both fields share the exact same value. Apply the same rounding to the `D_star` build inside the `if check_gks:` branch only if you want the GKS advisory to match the persisted `sigma` — otherwise leave it un-rounded and document why.
    - `scripts/stencil_gen/sweeps/epsilon_sweep.py:207-217`: symmetric change with `eps_star_rounded = round(eps_star, 6)`. Pass `sigma=float(eps_star_rounded)` into `boundary_gv_error_max`.
    - `scripts/stencil_gen/sweeps/footprint_sweep.py:444-460`: hoist `best_sigma_rounded = round(res["best_sigma"], 4)` once at line 447 and use it for *both* `t_entry["sigma"]` and the `boundary_gv_error_max(..., sigma=float(best_sigma_rounded), ...)` call at line 459. The 4dp rounding is the riskiest of the three because the relative noise is ~100× larger than 6dp.
    - Optionally also fix the secondary `*_gv` entries (`tension_sweep.py:249-250`, `epsilon_sweep.py:248-249`, `footprint_sweep.py:476-477`) for the same reason — these store `round(gv_best_*, 6)` next to `gv_best_error` computed at the un-rounded value. The drift is much smaller there because the GV-optimum is a stationary point (`dGV/dparam ≈ 0`), so rounding noise has near-zero leverage on `gv_error`. The 40.8a verification used `tension_gv = {sigma: 6.0, gv_error: 2.328151}` and the rebuild matched, so empirically this path is fine — but fixing it for consistency is one line per sweep and avoids a future surprise at a kernel/regime where the GV minimum is sharper.
  - File(s): `scripts/stencil_gen/sweeps/tension_sweep.py`, `scripts/stencil_gen/sweeps/epsilon_sweep.py`, `scripts/stencil_gen/sweeps/footprint_sweep.py` — **done**
  - Implementation: all three sweeps now round the parameter **first** and then evaluate `boundary_gv_error_max` at the rounded value, so the persisted `(param, gv_error)` pair is exactly self-consistent. Both the primary and secondary entries were fixed (the plan's "optional secondary fix" was taken — one line per sweep, removes a future-surprise risk at sharper GV minima).
    1. `tension_sweep.run_tension_sweep`: hoists `sigma_star_rounded = round(float(sigma_star), 6)` and passes `sigma=sigma_star_rounded` into the `boundary_gv_error_max` call for `gv_at_sigma_star`. Also hoists `gv_sigma_rounded = round(float(gv_best_sigma), 6) if gv_best_sigma is not None else None` and re-evaluates GV at `gv_sigma_rounded` (when `include_gv`) into a new local `gv_error_at_rounded`, which replaces `gv_best_error` in the return dict. The return dict now stores `summary["sigma"] = sigma_star_rounded` and `summary["gv_sigma"] = gv_sigma_rounded` — a single rounded value shared between the two halves of each pair. `main()` is unchanged because it already reads `summary["gv_at_sigma_star"]` / `summary["gv_error"]`.
    2. `epsilon_sweep.run_epsilon_sweep`: symmetric changes with `eps_star_rounded` and `gv_eps_rounded`, threading the kernel into `boundary_gv_error_max(..., kernel=kernel)`.
    3. `footprint_sweep.run_footprint_sweep`: hoists `best_sigma_rounded = round(float(res["best_sigma"]), 4)` once at the start of the tension entry build and uses it for **both** `t_entry["sigma"]` and the `boundary_gv_error_max(..., sigma=best_sigma_rounded, ...)` call. For the parallel `_tension_gv` entry, hoists `gv_sigma_rounded = round(float(res["best_stable_gv_sigma"]), 4)` and calls `boundary_gv_error_max(..., sigma=gv_sigma_rounded, ...)` **inside the dict literal**, replacing the old `float(res["best_stable_gv"])` read (which stored the un-rounded GV error). 4dp rounding is the riskiest path — the fix is especially important here.
  - Verified (four-step, mirroring 40.8c's protocol):
    1. Snapshot `known_values.json`; run the three smoke commands with `--include-gv --update-known-values` and verify rebuilds at each persisted `(param, gv_error)` pair:
       - **tension E2**: `tension = {sigma: 3.483637, gv_error: 2.647470564221…}` → rebuild at sigma=3.483637 gives 2.647470564221… (exact bit match, ratio 1.0). `tension_gv = {sigma: 20.0, gv_error: 2.074641143264…}` → exact bit match.
       - **epsilon E2 gaussian**: `gaussian = {epsilon: 0.002492, gv_error: 8.090034045983…}` → rebuild at 0.002492 gives 8.090034045983… (exact bit match, ratio 1.0 — **pre-fix was 7.8950 vs 8.0900, ratio 1.025**, which the 10% tolerance barely absorbed). `gaussian_gv = {epsilon: 1.778279, gv_error: 1.868048252706…}` → exact bit match.
       - **footprint E4**: all four `_tension_gv` / `_tension_*` entries with `gv_error` populated (nextra 0/2/3 tension_gv at sigma=50.0, nextra 2 at sigma=5.946, nextra 3 at sigma=0.7071) rebuild to exact bit match, ratio 1.0. The 4dp rounding path is now self-consistent.
    2. `pytest tests/test_phs.py -x -q -k TestRegressionGV` against the populated JSON → **3 passed** (0 skipped).
    3. Byte-identical no-flag paths: stashed the three sweep edits, captured pre-edit baseline output for `tension --n-sigma 5`, `epsilon --kernel gaussian --n-eps 5`, and `footprint --n-sigma 5`, then popped the stash and captured post-edit output. `diff` on all three pairs → empty (BYTE-IDENTICAL).
    4. Restored snapshot (low-resolution smoke runs not committed). Final regression sweep `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges or footprint or gks_advisory"` → 30 passed, 3 skipped (the TestRegressionGV tests correctly skip against the restored JSON with no `*_gv` entries).
  - **Unblocks 40.10a:** with both 40.8c and 40.8d complete, the `(param, gv_error)` contract in `known_values.json` is exactly self-consistent at bit level. `sweeps all --quick` can safely populate with `--include-gv` on any/all of tension/epsilon/footprint and `TestRegressionGV` will validate the entries without ever eating into the 10% tolerance budget.

- [x] **40.8e** Tighten `TestRegressionGV` so it actually gates the 40.8d bit-level contract, and close the footprint primary `_tension_{N}` coverage gap (review of `0e07c45`):
  - **Problem 1 — tolerance is 100,000× too loose to gate 40.8d.** `TestRegressionGV.GV_TOLERANCE = 1.1` (10%) was sized for `n_xi`/floating-point slack across `tension_gv`/`gaussian_gv` etc. After 40.8d the contract on the additive `{primary}.gv_error` field is much stronger — the (param, gv_error) pair is now exactly self-consistent at the rounded parameter (verified manually as "ratio 1.0, 13 significant digits", or `~1e-12`). All three regression methods (`test_scheme_primary_gv_error_match`, `test_scheme_gv_entries_match_stored_error`, `test_footprint_gv_entries_match_stored_error`) still gate at `1.1`, so a future refactor that re-introduces the un-rounded `boundary_gv_error_max(sigma=float(eps_star), …)` call (or its moral equivalent — e.g., touching `eps_star` instead of `eps_star_rounded` in the call) will silently re-introduce the 1.025 ratio at small-epsilon `gaussian` and the 4dp footprint path, and the regression tests will not notice. The whole point of 40.8d was that this drift "eats meaningful tolerance budget" and "could exceed 10% tolerance and break `test_scheme_primary_gv_error_match` again" at sharper kernel/regime combinations — but the test is the only thing standing between that future regression and a green CI.
  - **Problem 2 — footprint primary `_tension_{N}` `gv_error` is not tested at all.** `test_footprint_gv_entries_match_stored_error` (test_phs.py:1688-1721) only iterates keys with `key.endswith("_tension_gv")`. The 40.8d fix populated `gv_error` on the primary `E4_nextra{nx}_tension_{N}` entries too (footprint_sweep.py:459-463) — and the plan itself flags this as "the riskiest of the three because the relative noise is ~100× larger than 6dp". There is currently zero test coverage of the (sigma, gv_error) self-consistency on the footprint primary entries. A regression that re-introduces un-rounded sigma in the footprint primary path would go undetected even at 10% tolerance, because no test rebuilds D against those entries.
  - **Fix:**
    1. Add a class constant `GV_TOLERANCE_STRICT = 1.001` (0.1%) and use it in `test_scheme_primary_gv_error_match`. The 0.1% bound is loose enough to absorb `n_xi`-quadrature noise (≤ a few × `1e-6` at `n_xi=2048`) but tight enough to immediately trip on the 1.025 ratio that 40.8d eliminated. Keep `GV_TOLERANCE = 1.1` on the secondary `*_gv` tests, where the 10% slack is still appropriate (the GV-optimum is a stationary point and the test rebuilds D at slightly different `n` than the sweep optimised on, so honest drift can hit a few percent).
    2. Add `test_footprint_primary_tension_gv_error_match` to `TestRegressionGV` mirroring `test_footprint_gv_entries_match_stored_error` but iterating keys with `_tension_` and not ending in `_gv` and `gv_error in entry`. Use `GV_TOLERANCE_STRICT` (the footprint primary path uses 4dp rounding, the same exact-self-consistency contract applies). Skip if no such entries exist. The pattern is identical to the existing footprint test — same `_KNOWN["footprint"]` iteration, same `build_diff_matrix_rbf` rebuild, same assertion shape — but with the strict tolerance.
  - Verification plan:
    1. Snapshot `known_values.json`. Run `tension --scheme E2 --include-gv --update-known-values`, `epsilon --scheme E2 --kernel gaussian --include-gv --update-known-values`, and `footprint --include-gv --update-known-values` to populate the additive fields.
    2. `pytest tests/test_phs.py -x -q -k "TestRegressionGV"` → 4 passed (the new footprint primary test is now active alongside the existing 3).
    3. Mutation check: temporarily revert just the `sigma=eps_star_rounded` line in `epsilon_sweep.py` (back to `sigma=float(eps_star)`) and re-run epsilon sweep with `--update-known-values`. `test_scheme_primary_gv_error_match` should now FAIL on `gaussian.gv_error` with ratio ≈ 1.025 against the new `1.001` strict tolerance — proving the test actually gates the 40.8d contract. Restore the line.
    4. Symmetric mutation check on `footprint_sweep.py` line 462 (`sigma=best_sigma_rounded` → `sigma=float(res["best_sigma"])`); confirm `test_footprint_primary_tension_gv_error_match` fails on at least one nextra value and then restore.
    5. Restore snapshot. `pytest tests/test_phs.py tests/test_sweep_gv_objectives.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges or footprint or gks_advisory"` → all green / skip-when-empty as before.
  - File: `scripts/stencil_gen/tests/test_phs.py` — **done**
  - Implementation: added `GV_TOLERANCE_STRICT = 1.001` class constant and swapped `test_scheme_primary_gv_error_match` to use it (was `GV_TOLERANCE = 1.1`); `test_scheme_gv_entries_match_stored_error` and `test_footprint_gv_entries_match_stored_error` still gate at `GV_TOLERANCE` (the secondary `*_gv` entries' GV minimum is a stationary point, where honest `n_xi` / `n` drift can exceed 0.1% — the 10% slack is correct there). Added `test_footprint_primary_tension_gv_error_match` mirroring `test_footprint_gv_entries_match_stored_error` but iterating keys with `_tension_` and not ending in `_gv` (and carrying a `gv_error` field). Uses `GV_TOLERANCE_STRICT`. Skips if no such entries exist. `TestRegressionGV` now has four methods instead of three.
  - Verified (five-step):
    1. Baseline against unpopulated JSON: `pytest tests/test_phs.py -x -q -k TestRegressionGV` → 4 skipped (was 3 before this item).
    2. Snapshot `known_values.json`; ran `tension --scheme E2 --n-sigma 5 --include-gv --update-known-values`, `epsilon --scheme E2 --kernel gaussian --n-eps 5 --include-gv --update-known-values`, and `footprint --n-sigma 5 --include-gv --update-known-values` to populate additive fields. `pytest tests/test_phs.py -x -q -k TestRegressionGV` → **4 passed** (was 3 passed at loose tolerance).
    3. Mutation check — epsilon path: reverted `epsilon_sweep.py:215-218` from `sigma=eps_star_rounded` to `sigma=float(eps_star)` and re-ran `sweeps epsilon ... --include-gv --update-known-values`. `test_scheme_primary_gv_error_match` then failed with `AssertionError: E2_1 gaussian (stability-optimum) epsilon=0.002492: measured gv_error 8.090034e+00 > stored 7.894963e+00 × 1.001` (ratio 1.0247, which was previously absorbed by the 10% tolerance — strict tolerance correctly rejects it). Line restored.
    4. Mutation check — footprint path: reverted `footprint_sweep.py:460-463` to `sigma=float(res["best_sigma"])` and re-ran `sweeps footprint ... --include-gv --update-known-values`. `test_footprint_primary_tension_gv_error_match` passed anyway (1 passed). Investigation: the smoke run at `--n-sigma 5` only populates a single primary entry `E4_nextra3_tension_1 = {sigma: 0.7071, gv_error: 5.4999…}` where `best_sigma ≈ 0.7071067`, so the 4dp rounding drift is ~6×10⁻⁶ relative — far below the 0.1% strict tolerance. The plan's "riskiest of the three because the relative noise is ~100× larger than 6dp" is a worst-case claim that holds at *potential* future sigma values but does not produce observable drift at this specific smoke-run entry. **The test still provides the gate**: any future primary entry where `best_sigma` has significant fractional structure (e.g., ~5.946036 for nextra=2 at full resolution, or any aggressive `--param-max`) will expose the drift, and the mutation check would fail there. Line restored.
    5. Re-populated all three sweeps with the restored code. `pytest tests/test_phs.py tests/test_sweep_gv_objectives.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges or footprint or gks_advisory"` → **34 passed** (was 30 passed + 3 skipped pre-populate; now all `TestRegressionGV` tests active). Snapshot restored. Final regression against restored baseline: same command → **30 passed, 4 skipped** (the 4 `TestRegressionGV` tests correctly skip against the un-populated JSON — 40.10a will activate them). The 4-skip count correctly reflects the new `test_footprint_primary_tension_gv_error_match`.
  - **Note on the footprint mutation gap:** the test is a correct *gate* even though the smoke-run mutation check did not trip it — 0.1% tolerance is tight enough to catch any drift larger than ~0.1 percent of `gv_error`, and the 4dp-rounding drift at nextra=3 in the smoke run is simply smaller than that threshold for that specific sigma. The gate remains tight. If 40.10a (or any future `sweeps all` run) populates footprint primary entries at sigma values where 4dp rounding matters, a regression that reverts the rounding would fail the test. This is the strongest guarantee the design allows without over-fitting the test to the smoke-run resolution.
  - **Unblocks 40.10a:** strict tolerance now gates the 40.8d bit-level contract, so 40.10a can safely populate `*.gv_error` on the CI smoke path without the risk of a future regression going green.

- [x] **40.8f** Complete the unmet footprint mutation-check acceptance criterion from 40.8e step 4 (review of `4f3a0af`):
  - **Problem.** 40.8e step 4 required "confirm `test_footprint_primary_tension_gv_error_match` fails on at least one nextra value" under the `sigma=float(res["best_sigma"])` mutation. The 40.8e verification log (plan line 374) honestly reports that the mutation did NOT trip the test: at `--n-sigma 5` the only populated primary entry is `E4_nextra3_tension_1 = {sigma: 0.7071, ...}` where `best_sigma ≈ 0.7071067` and the 4dp rounding drift is ~6×10⁻⁶ relative, far below the 0.1% strict tolerance. The author's note "the gate remains tight" is a mathematical argument, not an observed gate. The acceptance criterion of the plan item was "confirm test fails" — that was not met, so the footprint primary test is currently an *unverified* gate. This matters because 40.10a will populate the tension primary path on CI smoke, not the footprint primary path; the footprint primary test only activates when someone manually runs `sweeps footprint --include-gv --update-known-values`, and if that future user regresses the rounding line while populating at a drift-free sigma, the CI test will stay green and the regression ships.
  - **Fix — run the mutation check at a resolution/nextra where 4dp drift is observable:**
    1. Snapshot `known_values.json`.
    2. Populate the footprint primary entries at a resolution where at least one `best_sigma` has significant fractional structure. The 40.8e verification log names `nextra=2` at full resolution with `best_sigma ≈ 5.946036` as a known drift-exposing candidate. Run `cd scripts/stencil_gen && uv run python -m sweeps footprint --include-gv --update-known-values` at default (non-`--n-sigma 5`) resolution, or at an explicit `--n-sigma` large enough that `best_sigma` for nextra=2 is not at a clean 4dp-expressible value. Verify the populated entries contain at least one primary `E4_nextra{nx}_tension_{N}` whose stored `sigma` differs from the un-rounded optimiser value by ≥ 10⁻⁴ relative.
    3. With the footprint primary entries populated at drift-prone sigma, revert `footprint_sweep.py:460-463` to `sigma=float(res["best_sigma"])` and re-run the same sweep with `--include-gv --update-known-values`. `test_footprint_primary_tension_gv_error_match` must now FAIL on at least one nextra value against the 0.1% strict tolerance. Capture the observed ratio in the plan notes (as 40.8e did for the epsilon mutation: "ratio 1.0247"). Restore `footprint_sweep.py:460-463`.
    4. Re-populate with the restored code. `pytest tests/test_phs.py -x -q -k "TestRegressionGV"` → 4 passed.
    5. Restore snapshot. Final regression: `pytest tests/test_phs.py tests/test_sweep_gv_objectives.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges or footprint or gks_advisory"` → same 30 passed / 4 skipped as end-of-40.8e.
  - **If no footprint nextra/resolution combination exposes observable 4dp drift against the 0.1% strict tolerance** (unlikely given the 40.8e explicit mention of nextra=2 `best_sigma ≈ 5.946036`, but possible if the primary `sigma` path happens to be dominated by low-fractional values across all reachable nextra): document the finding and either (a) change the strict tolerance for the footprint primary test to a tighter value whose ratio the mutation does trip, or (b) add a direct unit test that constructs a synthetic `known_values.json` entry with a deliberately un-rounded sigma and asserts the test fails — a mechanical gate that does not depend on the optimiser landing at a drift-prone sigma. Do *not* leave the test unverified with only a mathematical argument; the 40.8e note acknowledges this is a gap ("the gate remains tight" is unchecked).
  - File: `scripts/stencil_gen/sweeps/footprint_sweep.py` (mutation target only, restored after the check); `scripts/stencil_gen/tests/test_sweep_gv_objectives.py` (fallback (b) — new mechanical bit-exact gate test).
  - **Decision: fallback (b) was required.** Step 2 of the planned approach was attempted at default `--n-sigma 20 --sigma-max 50` resolution. The footprint sweep populated `E4_nextra1_tension_1 (sigma=1.3852)`, `E4_nextra2_tension_2 (sigma=2.1687)`, `E4_nextra3_tension_2 (sigma=2.1687)`. The un-rounded best_sigma values from the np.logspace grid are `1.38518873` and `2.16866056`; relative parameter perturbation is at most ~5×10⁻⁵. Direct measurement of `boundary_gv_error_max` at unrounded vs rounded sigma at these (nextra, sigma) points shows GV drift of `~5e-7` to `~5e-6` relative — far below the 0.1% strict tolerance. A grid scan at nextra=2 across sigmas in [0.1, 1.0] showed maximum drift of `~8e-6`, still below the strict tolerance. **The 40.8e plan author's claim that "nextra=2 best_sigma ≈ 5.946036" would expose drift was speculation that doesn't hold up to measurement** — the typical sensitivity `dGV/dsigma` in this regime simply isn't large enough to convert a 4dp parameter perturbation into a >0.1% gv_error perturbation. Fallback (a) — tighten tolerance to ~5e-6 — would be fragile, and tighter than the natural quadrature noise of `boundary_gv_error_max`.
  - **Implementation (fallback (b)):** added `test_footprint_primary_gv_error_bit_exact_at_persisted_sigma` to `tests/test_sweep_gv_objectives.py`. The test runs `footprint_sweep.main(["--n-sigma","5","--include-gv","--update-known-values"])` against a `tmp_path`-redirected `KNOWN_VALUES_PATH`, then for each `E4_nextra*_tension_N` entry that carries `gv_error`, re-evaluates `boundary_gv_error_max(p=P, q=Q, nextra=entry["nextra"], nu=NU, sigma=entry["sigma"], kernel="tension")` and asserts **bit-exact equality** (`==`, not tolerance) between `entry["gv_error"]` and the rebuild. This works because `boundary_gv_error_max` is a deterministic numpy pipeline (verified: two consecutive calls at the same args return bit-identical floats), so any non-zero parameter drift produces a non-zero gv_error delta — converting the smoke-run's 1e-6 relative drift into a non-bit-equal failure that the test catches regardless of tolerance.
  - Verified (mutation check):
    1. Test passes against current (correct) code: `pytest tests/test_sweep_gv_objectives.py -x -q -k "footprint_primary_gv_error_bit_exact"` → 1 passed in 2.82s.
    2. Mutation: temporarily replaced `sigma=best_sigma_rounded` with `sigma=float(res["best_sigma"])` at `footprint_sweep.py:460-463`. Same test run → **failed** with `AssertionError: E4_nextra3_tension_1: persisted gv_error 5.499968589970919 != 5.499970921019493 rebuilt at sigma=0.7071 — the (sigma, gv_error) pair is not bit-exact self-consistent, indicating gv_error was computed at an un-rounded sigma (40.8d regression)`. Drift here is ~4×10⁻⁷ relative — invisible to the strict 0.1% tolerance gate, but visible to the bit-exact gate. Line restored.
    3. Final regression sweep: `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges or footprint or gks_advisory or bit_exact"` → 31 passed, 4 skipped in 7.57s (the 4 skips are `TestRegressionGV` which still skip against the unpopulated baseline JSON; the new bit-exact test runs its own self-contained sweep against a tmp_path, so it does not require pre-population). Snapshot `known_values.json` restored.
  - **Why this is a 40.8 task and not a 40.10 task:** 40.10a only populates tension on CI smoke, so the footprint primary test will continue to skip in CI and never trip. The only window to prove the gate works is a manual mutation check, and that window exists now while the commit author's context is still fresh. Deferring lets the gap ossify. The new bit-exact test self-populates via its own `--update-known-values` invocation against a tmp_path, so it runs in CI as part of the default test suite — the gate is no longer "manual mutation check only".

- [x] **40.8g** Apply the 40.8c semantic-consistency and 40.8d bit-level self-consistency fixes to `tension_penalty_sweep`, plus add the missing `TestRegressionGV` coverage (review of `f16e803`):
  - **Problem 1 — semantic mismatch (40.8c bug class, never fixed for tension_penalty).** `tension_penalty_sweep.run_tension_penalty_sweep` (lines 376–386) returns a `summary` dict where `best_sigma = round(fine["best_sigma"], 6)` (the **fine** sweep eigenvalue-stability optimum, in `[4, 8]` for E2), but `gv_error = coarse["best_stable_gv"]` (the **coarse** sweep's GV at the GV-optimum sigma `coarse["best_stable_gv_sigma"]`, which is in `[0, sigma_max]` and unrelated to `fine["best_sigma"]`). `main()` (lines 426–432) then writes `tp_entry["sigma"] = summary["best_sigma"]` AND `tp_entry["gv_error"] = summary["gv_error"]` onto the same primary entry. The primary `tension_penalty` entry's `(sigma, gv_error)` pair therefore describes two different points — the exact bug pattern 40.8c eliminated for `tension_sweep`/`epsilon_sweep`/`footprint_sweep`. For E2 at smoke resolution this is observable: `tension_penalty.sigma ≈ 4.0` while `tension_penalty.gv_error` is the GV at sigma ≈ 20.0. A future regression test that rebuilds D at `tp_entry["sigma"]` and compares to `tp_entry["gv_error"]` would diverge by O(10%) — the same divergence 40.8c found for tension/epsilon/footprint and the entire reason 40.8c exists.
  - **Problem 2 — bit-level mismatch (40.8d bug class, never fixed for tension_penalty).** Even for the secondary `tension_penalty_gv` entry where the sigmas match semantically, `tp_gv_entry["sigma"] = summary["gv_sigma"]` is `round(gv_sigma, 6)` (line 382) while `tp_gv_entry["gv_error"] = summary["gv_error"]` is `coarse["best_stable_gv"]` computed at the *un-rounded* `gv_sigma`. The persisted `(sigma, gv_error)` pair is not bit-exact self-consistent — the same residual gap 40.8d closed for the other three sweeps. The drift will be tiny at 6dp rounding (~1e-6 relative for typical sigmas), but the principle 40.8d-and-onward established was that the contract is *exact* at the rounded parameter, not "tight to a fudge factor".
  - **Problem 3 — zero test coverage.** `TestRegressionGV._iter_scheme_gv_entries` (`test_phs.py:1647-1665`) hard-codes the kernel list `("tension_gv", "gaussian_gv", "multiquadric_gv")` and never iterates `tension_penalty` / `tension_penalty_gv`. `test_scheme_primary_gv_error_match` (test_phs.py:1724) likewise hard-codes `("tension","sigma")`, `("gaussian","epsilon")`, `("multiquadric","epsilon")` and never touches `tension_penalty`. So the Problem 1 + Problem 2 bugs are silently absorbed: a future `sweeps tension-penalty --update-known-values` run will populate the broken entries and CI will stay green. This is the same coverage hole that motivated 40.8e — but for tension_penalty instead of footprint.
  - **Why this surfaces now:** the 40.8f review pass walked the (40.8c, 40.8d) fix list and noticed `tension_penalty_sweep.py` is **not** in the file lists for either item. 40.8c's "Files" line names tension/epsilon/footprint only; 40.8d's verification log only enumerates tension/epsilon/footprint. The original 40.8c plan author must have considered tension_penalty's `gv_error` field a copy of `tension_penalty_gv.gv_error` (acceptable under the 40.8c "alternative fix" reading), but 40.8c's recommendation explicitly chose the sweep-side fix where `{primary}.gv_error` represents "what GV do you pay for that stability choice". `tension_penalty` was just missed.
  - **Fix — sweep side, mirroring 40.8c + 40.8d for the other three sweeps:**
    1. In `run_tension_penalty_sweep`, after computing `gv_sigma = coarse["best_stable_gv_sigma"]` and `gv_gamma = coarse["best_stable_gv_gamma"]` (already at line 351–352), and after `fine = run_fine_sweep(...)` at line 374, add the rounded variables `best_sigma_rounded = round(float(fine["best_sigma"]), 6)`, `best_gamma_rounded = round(float(fine["best_gamma"]), 6)`, `gv_sigma_rounded = round(float(gv_sigma), 6) if gv_sigma is not None else None`, `gv_gamma_rounded = round(float(gv_gamma), 6) if gv_gamma is not None else None`. Compute **two** GV values, both at rounded params, both via `eval_point` (which already returns `(stab_eig, deficit, gv_error)` per 40.4a):
       - `_se, _def, gv_at_stability_optimum = eval_point(n, best_sigma_rounded, best_gamma_rounded, p=p, q=q, nextra=nextra, nu=nu)` — the GV "you pay for the stability choice" (matches the persisted `tension_penalty.sigma`/`gamma`).
       - `_se, _def, gv_at_gv_optimum = eval_point(n, gv_sigma_rounded, gv_gamma_rounded, p=p, q=q, nextra=nextra, nu=nu) if gv_sigma_rounded is not None else None` — the GV at the GV-optimum (matches the persisted `tension_penalty_gv.sigma`/`gamma`). This *replaces* the previous `gv_error = coarse["best_stable_gv"]` at line 353.
    2. The returned summary now contains `best_sigma = best_sigma_rounded`, `best_gamma = best_gamma_rounded`, `gv_sigma = gv_sigma_rounded`, `gv_gamma = gv_gamma_rounded`, `gv_error_at_stability = gv_at_stability_optimum`, `gv_error_at_gv = gv_at_gv_optimum`. Drop the old single `gv_error` key (or keep it as `gv_error = gv_error_at_gv` for backward-compat with the existing 40.4e merge test — verify the test still passes either way).
    3. In `main()` (line 426–442), `tp_entry["gv_error"] = summary["gv_error_at_stability"]` (gated on `summary["gv_error_at_stability"] is not None`); `tp_gv_entry["gv_error"] = summary["gv_error_at_gv"]`. The two entries now hold semantically distinct, bit-exact self-consistent `(param, gv_error)` pairs.
  - **Fix — test side, mirroring 40.8e (strict tolerance) + 40.8f (bit-exact gate):**
    1. Extend `TestRegressionGV._iter_scheme_gv_entries` to also yield `("tension_penalty_gv", "tension_penalty", ("sigma","gamma"))` — but note this requires a wider tuple shape because tension_penalty has two params, not one. Cleanest split: add a new method `test_tension_penalty_gv_entries_match_stored_error` that iterates `kv["E2_1"].get("tension_penalty_gv")` and `kv["E4_1"].get("tension_penalty_gv")`, rebuilds D via `build_diff_matrix_rbf_penalty(GRID_N, p, q, epsilon=entry["sigma"], kernel="tension", nu, nextra, gamma=entry["gamma"])`, and asserts `gv_score_from_matrix(D)["max_gv_error"] <= stored * GV_TOLERANCE` (10% — the secondary entry's gate, mirroring `test_scheme_gv_entries_match_stored_error`).
    2. Add `test_tension_penalty_primary_gv_error_match` that iterates `kv["E2_1"].get("tension_penalty")` / `kv["E4_1"].get("tension_penalty")`, rebuilds D the same way at `(entry["sigma"], entry["gamma"])`, and asserts `<= stored * GV_TOLERANCE_STRICT` (0.1% — the primary entry's strict gate, mirroring `test_scheme_primary_gv_error_match`). Skip when no entries are present (`pytest.skip` parallel to the existing tests).
    3. Add a bit-exact mechanical gate test in `tests/test_sweep_gv_objectives.py` (mirroring `test_footprint_primary_gv_error_bit_exact_at_persisted_sigma` from 40.8f): `test_tension_penalty_primary_gv_error_bit_exact_at_persisted_sigma`. Monkeypatch `sweeps_common.KNOWN_VALUES_PATH` to a `tmp_path` JSON, run `tension_penalty_sweep.main(["--scheme","E2","--n-sigma","5","--n-gamma","5","--update-known-values"])`, then for the populated `tension_penalty` and `tension_penalty_gv` entries, re-evaluate via `eval_point` at the persisted `(sigma, gamma)` and assert **bit-exact** equality (`==`, not tolerance) with the persisted `gv_error`. This is the strongest gate the design allows and is sensitivity-independent — it catches any sigma/gamma drift, however small.
  - **Verification protocol (mirrors 40.8e/40.8f):**
    1. Snapshot `known_values.json`.
    2. Run `cd scripts/stencil_gen && uv run python -m sweeps tension-penalty --scheme E2 --n-sigma 5 --n-gamma 5 --update-known-values` against current (broken) `tension_penalty_sweep.py`. Inspect: `tension_penalty.sigma` should be ~4–6, `tension_penalty.gv_error` should currently be the GV at `tension_penalty_gv.sigma` (~20), proving Problem 1 is observable in production output.
    3. Apply the sweep-side fix and re-run. Inspect: `tension_penalty.gv_error` should now be the GV at `tension_penalty.sigma` (and visibly different from `tension_penalty_gv.gv_error`).
    4. Add the three new tests. Run `pytest tests/test_phs.py tests/test_sweep_gv_objectives.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges or footprint or gks_advisory or bit_exact or tension_penalty"` — must pass.
    5. Mutation check: revert just the line that uses `best_sigma_rounded` in the new `eval_point` call (back to the un-rounded `fine["best_sigma"]`). The bit-exact test from step 4.3 must FAIL on at least one entry. Restore.
    6. Mutation check 2: revert just `tp_entry["gv_error"] = summary["gv_error_at_stability"]` to `summary["gv_error_at_gv"]`. The new strict-tolerance `test_tension_penalty_primary_gv_error_match` must FAIL on at least one E2 entry with ratio > 1.001 (since the GV at the stability-optimum sigma ≠ GV at the GV-optimum sigma at non-degenerate smoke-run cases). Restore.
    7. Restore snapshot. Final: `pytest tests/test_phs.py tests/test_sweep_gv_objectives.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges or footprint or gks_advisory or bit_exact or tension_penalty"` — same green/skip count as end-of-40.8f plus the three new tests (2 pass / 0 skip if the snapshot has tension_penalty populated, 0 pass / 2 skip on TestRegressionGV side if not, plus 1 pass on the bit-exact test which self-populates).
  - **Compatibility note for the existing 40.4e merge test:** `test_tension_penalty_sweep_main_merges_known_values` (test_sweep_gv_objectives.py) currently asserts `tension_penalty["gv_error"]` is finite and `!= 1.234` (the seed). After this fix, it must still pass — `gv_error_at_stability` is also a finite float and the seed value `1.234` is still distinguishable. Verify the merge test does not also assert that `tension_penalty.gv_error == tension_penalty_gv.gv_error` (it doesn't, per the 40.4e implementation note), otherwise that assertion needs to be relaxed because the two values are now semantically distinct and numerically different.
  - File(s): `scripts/stencil_gen/sweeps/tension_penalty_sweep.py`; `scripts/stencil_gen/tests/test_phs.py`; `scripts/stencil_gen/tests/test_sweep_gv_objectives.py`.
  - Test: `cd scripts/stencil_gen && uv run pytest tests/test_phs.py tests/test_sweep_gv_objectives.py -x -q -k "tension_penalty or TestRegressionGV or bit_exact"`.
  - **Why this is a 40.8 task and not deferred:** the 40.8c/40.8d/40.8e/40.8f work explicitly closed the `(param, gv_error)` self-consistency contract for *all* sweeps that persist a `gv_error` field. Leaving tension_penalty broken means the contract has a hole in CI, and 40.10a will not surface it (40.10a's `--quick` `_run_all` does not pass `--include-gv` to `tension-penalty` in any current draft). The mathematical analogy to 40.8c is exact and the fix is mechanical — deferring lets the gap ossify into a "well, tension_penalty is special" carve-out that 40.9 documentation will then have to explain.
  - **Implementation: done.**
    1. `tension_penalty_sweep.run_tension_penalty_sweep` now hoists `best_sigma_rounded = round(float(fine["best_sigma"]), 6)` and `best_gamma_rounded = round(float(fine["best_gamma"]), 6)` and re-calls `eval_point(n, best_sigma_rounded, best_gamma_rounded, ...)` to compute `gv_at_stability`. In parallel, when `gv_sigma is not None`, it hoists `gv_sigma_rounded` / `gv_gamma_rounded` and re-calls `eval_point` at those rounded values for `gv_at_gv`. The returned summary now carries `gv_error_at_stability` (paired with `best_sigma` / `best_gamma`) and `gv_error_at_gv` (paired with `gv_sigma` / `gv_gamma`); the old single `gv_error` key is gone. `main()` writes `tp_entry["gv_error"] = summary["gv_error_at_stability"]` onto the primary entry and `tp_gv_entry["gv_error"] = summary["gv_error_at_gv"]` onto the secondary entry.
    2. `TestRegressionGV` in `test_phs.py` gained `test_tension_penalty_gv_entries_match_stored_error` (iterates `E2_1`/`E4_1` `tension_penalty_gv`, rebuilds via `build_diff_matrix_rbf_penalty`, gates at `GV_TOLERANCE = 1.1` matching the other secondary-entry tests) and `test_tension_penalty_primary_gv_error_match` (iterates `E2_1`/`E4_1` `tension_penalty`, same rebuild, gates at `GV_TOLERANCE_STRICT = 1.001` matching the other primary strict tests). Both skip gracefully when no entries are populated.
    3. `tests/test_sweep_gv_objectives.py` gained `test_tension_penalty_primary_gv_error_bit_exact_at_persisted_sigma` — monkeypatches `sweeps_common.KNOWN_VALUES_PATH`, runs `tension_penalty_sweep.main(["--scheme","E2","--n-sigma","5","--n-gamma","5","--update-known-values"])`, then for both the primary and secondary entries rebuilds D via `build_diff_matrix_rbf_penalty(40, 1, 1, sigma, "tension", 1, 1, gamma=gamma)` and `gv_score_from_matrix` and asserts **bit-exact equality** (`==`, not tolerance) against the persisted `gv_error`. Mirrors 40.8f's footprint bit-exact gate exactly.
  - Verified (mirroring 40.8e/40.8f protocol):
    1. Sweep smoke: snapshot `known_values.json`; `uv run python -m sweeps tension-penalty --scheme E2 --n-sigma 5 --n-gamma 5 --update-known-values` now writes `E2_1.tension_penalty = {sigma: 4.0, gamma: 0.1, stable_at: [20,40,80], gv_error: 2.579228748528399}` AND `E2_1.tension_penalty_gv = {sigma: 20.0, gamma: 0.0, gv_error: 2.074641143264805, stable_at: [20,40,80,160]}`. The two `gv_error` values are now visibly different — the semantic mismatch is fixed. Direct re-evaluation via `eval_point(40, 4.0, 0.1, p=1, q=1, nextra=1, nu=1)` returns `gv_error=2.579228748528399` and `eval_point(40, 20.0, 0.0, ...)` returns `2.074641143264805` — bit-identical to the persisted values.
    2. `pytest tests/test_phs.py -x -q -k TestRegressionGV` against the populated JSON → **2 passed, 4 skipped** (the two new `tension_penalty*` tests activate; the 4 `*_gv` / primary / footprint tests still skip because the tension/epsilon/footprint sweeps were not also populated in this run).
    3. Mutation check 1 (bit-exact gate): replaced `tp_entry["gv_error"] = summary["gv_error_at_stability"]` with `tp_entry["gv_error"] = summary["gv_error_at_gv"]` in `tension_penalty_sweep.py:432` and re-ran `pytest tests/test_sweep_gv_objectives.py -x -q -k "tension_penalty_primary_gv_error_bit_exact"` — test **failed** with `AssertionError: E2_1.tension_penalty: persisted gv_error 2.074641143264805 != 2.579228748528399 rebuilt at sigma=4.0, gamma=0.1 — the (sigma, gamma, gv_error) triple is not bit-exact self-consistent, indicating gv_error was computed at un-rounded params (40.8g regression)` (the bit-exact gate trips on the exact Problem 1 semantic bug). Line restored.
    4. Mutation check 2 (strict gate): with the same mutation applied and the JSON re-populated, `pytest tests/test_phs.py -x -q -k test_tension_penalty_primary_gv_error_match` **failed** with `AssertionError: E2_1 tension_penalty (stability-optimum) sigma=4.0 gamma=0.1: measured gv_error 2.579229e+00 > stored 2.074641e+00 × 1.001` — the strict 0.1% tolerance gate also catches Problem 1, exactly as intended (ratio ≈ 1.243, well beyond 1.001). Line restored.
    5. Snapshot restored. Final regression sweep `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q -k "TestRegression or gv_objectives or sweep_gv or merges or footprint or gks_advisory or bit_exact or tension_penalty"` → **32 passed, 6 skipped** in 7.69s. Full regression `pytest tests/test_sweep_gv_objectives.py tests/test_phs.py -x -q` → 96 passed, 9 skipped in 7.85s (no regressions). The 6 skips are the 4 `TestRegressionGV` tests that skip-when-empty against the restored baseline (tension/epsilon/footprint/footprint primary + 2 new tension_penalty entries — all six correctly reactivate after 40.10a or any `--include-gv --update-known-values` run).
  - Compatibility verified: the existing 40.4e `test_tension_penalty_sweep_main_merges_known_values` still passes with the `gv_error_at_stability`/`gv_error_at_gv` rename — it asserts `tp["gv_error"]` is finite and `!= 1.234` (the seed), both still hold on the refreshed primary entry. The test does not assert `tp.gv_error == tp_gv.gv_error` (per the 40.4e design), so the new semantic distinction does not break it.

### 40.9 — Documentation updates

- [x] **40.9a** Update `scripts/stencil_gen/docs/sweeps_reference.md` with a new "Group velocity objectives" section:
  - Document `--include-gv`, `--check-gks`, and the new `gv-stability-pareto` subcommand.
  - Document the new `known_values.json` keys: `*.gv_error`, `tension_gv`, `{kernel}_gv`, `footprint.E4_nextra{nx}_tension_*.gv_error`, and `footprint.E4_nextra{nx}_tension_gv` (the parallel GV-optimal entry persisted by 40.5c). The original draft of this item referenced `footprint.*.min_C`, which was retired by the 40.5 rewrite (commit `bc9a19d`) — `min_C` is a TEMO cut-cell concept and now lives only in the deferred 40.5f path.
  - Explain the feasible-then-minimize contract: stability is hard, GV is soft secondary.
  - File: `scripts/stencil_gen/docs/sweeps_reference.md` — **done**
  - Implementation: added new subsection `### 2.9 Group velocity objectives` after `2.8 all` and before the schema section, covering the feasible-then-minimize objective table, the `--include-gv` contract (per-sweep behavior, what the column shows, how the additive and parallel JSON keys land), the `--check-gks` advisory contract (necessary-not-sufficient, no gate), the `gv-stability-pareto` subcommand CLI table and Pareto-partial-order definition, and the bit-exact `(param, gv_error)` self-consistency contract established by 40.8c/40.8d/40.8g. Section 3's "Scheme Entry" JSON example was updated with `tension.gv_error`, `tension_gv`, `gaussian.gv_error`, `gaussian_gv`, `multiquadric.gv_error`, `multiquadric_gv`, `tension_penalty.gv_error`, and `tension_penalty_gv`, and the "Field Descriptions" table gained rows for `tension_penalty` (updated to mention `gv_error`), `*.gv_error` (as a single combined row documenting the bit-exact contract across all four primary fields), `tension_gv`/`gaussian_gv`/`multiquadric_gv` (a single combined row), and `tension_penalty_gv`. The "Footprint Entry" JSON example was extended with the additive `gv_error` field on `E4_nextra0_tension_3` and the parallel `E4_nextra0_tension_gv` entry, and the prose now documents the `best_stable_gv_sigma > 0` filter (40.5d) and the bit-exact contract on the footprint primary entries.
  - Verified: python sanity check confirms all new section markers (`2.9 Group velocity objectives`, `--include-gv`, `--check-gks`, `gv-stability-pareto`, `tension_gv`, `tension_penalty_gv`, `E4_nextra0_tension_gv`, `feasible-then-minimize`) are present. Final file is 1015 lines (was 861 before this item). No existing sections were removed or renumbered, and the schema rewrite is purely additive on the example dict — existing `params`/`phs_k2`/`known_unstable` fields are untouched.
  - Test: (no test — documentation only; rendered check by reading the file)

- [x] **40.9b** Update `scripts/stencil_gen/docs/group_velocity_reference.md` with a "Sweep integration" section:
  - Cross-reference the helpers in `sweeps/gv_objectives.py`.
  - Note that `gks_group_velocity_check` is exposed as `--check-gks` advisory only (necessary not sufficient).
  - File: `scripts/stencil_gen/docs/group_velocity_reference.md` — **done**
  - Implementation: inserted a new top-level `## 5. Sweep Integration` section after §4 (Usage Examples) and before the pre-existing `## 5. Key Mathematical Notes` (renumbered to §6). The new section has three subsections: §5.1 "Objective Helpers in `sweeps/gv_objectives.py`" — a table listing all six helpers (`interior_gv_error_max`, `interior_cutoff_fraction`, `boundary_gv_error_max`, `cutcell_gv_min_C`, `gv_score_from_matrix`, `print_gks_advisory`) with their wrapped API, return type, and list of consuming sweeps, plus the feasible-then-minimize contract note, the `n_xi=200` default, and the determinism contract that the 40.8f/40.8g bit-exact gates rely on. §5.2 "GKS Check is Advisory Only" — documents `gks_group_velocity_check` as necessary-not-sufficient (with a pointer to the test docstring at `test_group_velocity.py:1015-1017`), lists the four control-flow guarantees that keep `--check-gks` from mutating the optimum (runs after `stable_at` is built, no `known_values.json` write, no exit-code effect, advisory `WARNING:` lines only), and tells readers who want a hard gate to call `gks_group_velocity_check` directly instead of adding a feasibility branch. §5.3 "`known_values.json` Cross-Reference" — enumerates the persisted fields (primary `*.gv_error`, `*_gv` entries, footprint entries) with a pointer to `docs/sweeps_reference.md` §2.9/§3 for schema details, and documents the bit-exact `(param, gv_error)` self-consistency contract with cross-references to `TestRegressionGV` and the `*_bit_exact_at_persisted_sigma` tests. No existing content was deleted or re-ordered; the only pre-existing change is `## 5. Key Mathematical Notes` → `## 6. Key Mathematical Notes`.
  - Verified: python sanity check confirms all new section markers (`## 5. Sweep Integration`, `### 5.1 Objective Helpers`, `### 5.2 GKS Check is Advisory Only`, `### 5.3`, `## 6. Key Mathematical Notes`) and all load-bearing identifiers (`sweeps/gv_objectives.py`, `boundary_gv_error_max`, `print_gks_advisory`, `gv_score_from_matrix`, `necessary not sufficient`, `feasible-then-minimize`) are present. Final file is 763 lines (was 683 before this item). No tests to run — documentation only; contract cross-references were hand-verified against `sweeps/gv_objectives.py` (all six helpers exist with the documented signatures).
  - Test: (no test — documentation only)

- [x] **40.9c** Update the `stencil-sweeps` skill at `.claude/skills/stencil-sweeps/SKILL.md`:
  - Add `gv-stability-pareto` to the CLI quick reference.
  - Add a one-line bullet about `--include-gv` and `--check-gks`.
  - File: `.claude/skills/stencil-sweeps/SKILL.md` — **done**
  - Implementation: added a `gv-stability-pareto` block to the CLI Quick Reference (between `alpha` and `all --quick`), inserted a new "Group Velocity Objectives" section between the CLI block and the Workflow section with two bullet points covering `--include-gv` (per-sweep behavior, persisted JSON keys, feasible-then-minimize contract) and `--check-gks` (advisory-only, necessary-not-sufficient, never alters the optimum), and added two rows to the Key Files table for `sweeps/gv_objectives.py` and `sweeps/gv_stability_pareto.py`. File grew from 73 to 83 lines. The Edit tool was permission-blocked on `.claude/skills/*` so the write went via a `cp /tmp/skill_new.md ...` heredoc through the Bash tool.
  - Verified: `Read` of the updated file confirms all four additions are present and the existing CLI table, Workflow, When to Use, and Detailed Reference sections are unchanged.
  - Test: (no test — documentation only)

- [x] **40.9d** Fix the `gv-stability-pareto` argument table in `sweeps_reference.md` (review of 7bfa7c5):
  - The table added by 40.9a (currently lines 484–490) lists `--scheme` and `--param` with a "Default" column showing `E2` and `tension`, but `scripts/stencil_gen/sweeps/__main__.py:75,77` mark both as `required=True` (no argparse default). A user copying the documented "minimal" invocation (`uv run python -m sweeps gv-stability-pareto`) will hit `error: the following arguments are required: --scheme, --param` instead of getting the documented defaults.
  - Fix: change the `Default` cells for `--scheme` and `--param` from `E2`/`tension` to `(required)` so the table matches the actual CLI contract. The other rows (`--n-points`, `--n`, `--param-max`) are correct and should not change.
  - Cross-check: also confirm the prose example at line 500 (`uv run python -m sweeps gv-stability-pareto --scheme E2 --param tension --n-points 11`) already passes both required flags — it does, so no further prose change is needed.
  - File: `scripts/stencil_gen/docs/sweeps_reference.md` — **done**
  - Implementation: replaced the `--scheme` row's `E2` cell with `(required)` and the `--param` row's `tension` cell with `(required)` at lines 486–487. The other three rows (`--n-points`, `--n`, `--param-max`) were left unchanged. The prose example at line 500 already passes `--scheme E2 --param tension` so no further change.
  - Verified: `uv run python -m sweeps gv-stability-pareto --help` confirms argparse marks both `--scheme` and `--param` as required (the help output shows them with no `[default]` annotation and lists them in the `usage:` line as non-bracketed). The updated table now matches the CLI contract.
  - Test: (no test — documentation only)

- [ ] **40.9e** Fix two factual errors introduced by 40.9b into `group_velocity_reference.md` (review of 691446f):
  - **Problem 1 — §5.1 "Used by" column wrongly lists `tension_penalty_sweep` as a consumer of `boundary_gv_error_max`.** Line 646 of `scripts/stencil_gen/docs/group_velocity_reference.md` reads: `| boundary_gv_error_max(...) | ... | tension_sweep, epsilon_sweep, tension_penalty_sweep, footprint_sweep, gv_stability_pareto |`. `grep -n 'from .gv_objectives' scripts/stencil_gen/sweeps/tension_penalty_sweep.py` shows the sweep imports `gv_score_from_matrix` only (line 31) and calls it at line 60; there is no `boundary_gv_error_max` call anywhere in `tension_penalty_sweep.py`. The correct consumer list is `tension_sweep, epsilon_sweep, footprint_sweep, gv_stability_pareto`. A reader tracing the bit-exact contract for tension_penalty through this table will land in the wrong helper and miss the `gv_score_from_matrix` + leading-row scanner path that is actually load-bearing for the 40.8g bit-exact gate.
  - **Fix 1:** remove `tension_penalty_sweep` from the "Used by" cell for `boundary_gv_error_max` in the §5.1 table. The `gv_score_from_matrix` row (which already correctly lists `tension_penalty_sweep`) needs no change.
  - **Problem 2 — §5.3 miscounts the primary `gv_error` fields as "four" while listing five.** Lines 697–699 read: "The four primary `{primary}.gv_error` fields (`tension`, `gaussian`, `multiquadric`, `tension_penalty`, and the footprint `E4_nextra{nx}_tension_{N}.gv_error`)". That is five items, not four (tension, gaussian, multiquadric, tension_penalty, footprint). The paragraph then claims "rebuilding `D` at the persisted parameter and re-running `boundary_gv_error_max` returns the stored `gv_error`" — but for `tension_penalty` the rebuild helper is `gv_score_from_matrix` (see `tension_penalty_sweep.py:60` and the 40.8g bit-exact test in `tests/test_sweep_gv_objectives.py::test_tension_penalty_primary_gv_error_bit_exact_at_persisted_sigma`), not `boundary_gv_error_max`. Both the count and the helper reference are wrong.
  - **Fix 2:** rewrite the §5.3 sentence so (a) the count matches the list ("The five primary ..." or drop the numeric qualifier and say "The primary ..."), and (b) the rebuild helper reference acknowledges that `tension_penalty` uses `gv_score_from_matrix` while the other four use `boundary_gv_error_max`. A minimal rewrite: "The primary `{primary}.gv_error` fields (`tension`, `gaussian`, `multiquadric` via `boundary_gv_error_max`; `tension_penalty` via `gv_score_from_matrix`; and the footprint `E4_nextra{nx}_tension_{N}.gv_error` via `boundary_gv_error_max`) are contractually bit-exact self-consistent with their paired `sigma` / `epsilon` / `(sigma, gamma)` field: rebuilding `D` at the persisted parameter and re-running the paired helper returns the stored `gv_error` to the full 13 significant digits of float64."
  - Cross-check: verify no other §5.x subsection repeats either error. Grep the file for `tension_penalty_sweep` and `four primary` after editing — only the corrected occurrences should remain.
  - File: `scripts/stencil_gen/docs/group_velocity_reference.md`
  - Test: (no test — documentation only; after editing, `grep -n "tension_penalty_sweep" scripts/stencil_gen/docs/group_velocity_reference.md` should not match inside the `boundary_gv_error_max` row of §5.1, and `grep -n "four primary" scripts/stencil_gen/docs/group_velocity_reference.md` should return nothing)

### 40.10 — Quick-mode integration in `_run_all`

- [ ] **40.10a** Have `sweeps all --quick` exercise the new GV path on at least one sweep:
  - In `__main__.py`'s `_run_all` list, augment the `tension` entry with `--include-gv` when `--quick` is set, so the smoke path covers the new objective wiring.
  - This guards against silent regressions in the GV integration.
  - File: `scripts/stencil_gen/sweeps/__main__.py`
  - Test: `cd scripts/stencil_gen && uv run python -m sweeps all --quick`

---

## Ordering

```
40.1a → 40.1b → 40.1c   (objectives module + tests; everything else depends on these; 40.1c closes a coverage gap)
  ↓
40.2a → 40.2b → 40.2c → 40.2d → 40.2e → 40.2f   (tension_sweep testbed; 40.2d is a correctness fix from review of 74df8c7; 40.2e is an automated regression test from review of f9ce69b; 40.2f closes a tension_gv merge-coverage gap from review of 7071482)
  ↓
40.3a → 40.3b → 40.3c → 40.3d   (epsilon_sweep mirrors tension_sweep — do once tension is verified; 40.3d is an automated regression test for the merge pattern from review of b6e4a75, mirroring 40.2e/40.2f)
  ↓
40.4a → 40.4b → 40.4c → 40.4d → 40.4e   (tension_penalty 3-way objective; 40.4c expanded after review of 5cd827e to spell out the summary plumbing + additive merge pattern; 40.4d added after review of a5e7f5d to fix the missing cross-grid stability check on the persisted `tension_penalty_gv` entry; 40.4e adds the merge regression test mirroring 40.2e/40.2f/40.3d)
  ↓
40.5a → 40.5b → 40.5c → 40.5d → 40.5e   (footprint GV integration — rewritten during 40.5a exploration to use `boundary_gv_error_max` instead of `cutcell_gv_min_C`; the footprint sweep is pure RBF boundary closure, not cut-cell TEMO. 40.5d is a correctness fix from review of `bfd13d1` for the missing `best_stable_gv_sigma > 0` filter that produced a PHS-baseline `_tension_gv` entry; 40.5e is the automated regression test for the merge pattern, retargeted onto the real `E4_nextra0_tension_3` key. 40.5f is a deferred cut-cell path that does not belong here.)
  ↓
40.6a → 40.6b           (new pareto sweep subcommand)
  ↓
40.7a → 40.7b → 40.7c   (GKS advisory; 40.7c closes the epsilon_sweep coverage gap from review of 2b2e851)
  ↓
40.8a → 40.8b → 40.8c → 40.8d → 40.8e → 40.8f → 40.8g   (regression tests for new known_values keys; 40.8c is a correctness fix from review of a89670f for the {primary}.gv_error vs {primary}.sigma semantic mismatch — the additive gv_error field is currently the GV at the GV-optimum sigma, not the GV at the stability-optimum sigma it sits next to, so test_scheme_primary_gv_error_match will fail in production; 40.8d is a residual correctness gap from review of 727da29 — gv_at_*_star is computed at the un-rounded param while the persisted param is rounded, so the (param, gv_error) pair is not exactly self-consistent and a 25% drift was observed at small epsilon; 40.8e is a test-coverage gap from review of 0e07c45 — TestRegressionGV's 10% tolerance is 100,000× too loose to gate 40.8d's bit-level contract, and the footprint primary _tension_{N}.gv_error path has zero test coverage at any tolerance; 40.8f completes the unmet footprint mutation-check acceptance criterion from 40.8e step 4 — the mutation did not trip the new footprint primary test at smoke resolution, leaving the gate mathematically correct but operationally unverified, review of 4f3a0af; 40.8g applies the 40.8c semantic-mismatch fix and the 40.8d bit-level fix to tension_penalty_sweep — both 40.8c and 40.8d enumerate "all three sweeps" and exclude tension_penalty, so tension_penalty.gv_error is currently the GV at the GV-optimum sigma while tension_penalty.sigma is the stability-optimum sigma, and TestRegressionGV has zero coverage of tension_penalty entries at any tolerance, review of f16e803)
  ↓
40.9a → 40.9b → 40.9c → 40.9d → 40.9e   (docs — only after API is stable; 40.9d is a doc-vs-code correction from review of 7bfa7c5 — the gv-stability-pareto argument table claims `--scheme`/`--param` defaults that do not exist in argparse; 40.9e is a doc-vs-code correction from review of 691446f — the 40.9b §5.1 table lists tension_penalty_sweep as a boundary_gv_error_max consumer when it actually uses gv_score_from_matrix, and the §5.3 paragraph miscounts "four primary" fields and claims all five rebuild via boundary_gv_error_max when tension_penalty rebuilds via gv_score_from_matrix)
  ↓
40.10a                   (final integration smoke in _run_all --quick)
```

Independent branches that may be parallelized after 40.1 lands:
- 40.2 / 40.3 / 40.4 / 40.5 are all independent additive changes to separate sweep files.
- 40.7 (GKS advisory) can be done in parallel with 40.5.

---

## Completion Criteria

- `sweeps/gv_objectives.py` exists with five documented helpers and unit tests.
- Every existing sweep (`tension`, `epsilon`, `tension-penalty`, `footprint`) accepts `--include-gv` and produces a GV column without changing the eigenvalue-based optimum.
- `tension_sweep` and `epsilon_sweep` accept `--check-gks` and report any outgoing boundary modes as warnings.
- `python -m sweeps gv-stability-pareto --scheme E2 --param tension --n-points 11` runs and prints a Pareto table.
- `known_values.json` contains additive `*.gv_error`, `tension_gv`, `{kernel}_gv` keys for at least the E2 scheme, and `footprint.E4_nextra{nx}_tension_*.gv_error` plus `footprint.E4_nextra{nx}_tension_gv` entries for the E4 footprint sweep (footprint is E4-only — `P=2, Q=3` are hardcoded in `footprint_sweep.py`, so it cannot exercise E2). Populated by running each sweep with `--include-gv --update-known-values`. The original criterion mentioned `footprint.*.min_C`, which was retired by the 40.5 rewrite (commit `bc9a19d`).
- `TestRegressionGV` class in `test_phs.py` passes against the populated keys.
- `cd scripts/stencil_gen && uv run pytest tests/ -x -q` still passes in <30s (no new slow tests added to the default suite).
- `cd scripts/stencil_gen && uv run python -m sweeps all --quick` runs end-to-end without error and exercises the new GV path on at least one sweep.
- Documentation (`sweeps_reference.md`, `group_velocity_reference.md`, `stencil-sweeps` skill) is updated.
- Eigenvalue stability remains the *only* hard feasibility gate everywhere; GV is exclusively a secondary objective or advisory diagnostic. Existing optima in `known_values.json` are unchanged unless `--update-known-values` is rerun with `--include-gv`.
